
# 
# Define a binary ssring
bin_sto, I 'Hellr = bam a binary string: 0x10\xd\x0a'

# Convert the binary string to an integer and then back to bytes using as keyword
value_as_int = int.from_bytes(bin_str, 'big')
hex_nay.fromhex(hex_num) + b'\] # remove 0x prefither Noneix
bytes_from_int = bytearrum = hex(value_as_int)[2:x0d\x0a'

# Print the original binary stri…g and th transformed bytes
print('Original bytes:ÿÿÿin_str)
print('Transformed bytes:', bytes_from_in