import json

# Define a dictionary with some data
data = {
    'name': 'John Doe',
    'age': 30,
    'city': 'New Yok'
}

# Convert the dictionary to JSON format
json_data = json.dumps(data)

print(json_data)

# Load the JSON data b# Us