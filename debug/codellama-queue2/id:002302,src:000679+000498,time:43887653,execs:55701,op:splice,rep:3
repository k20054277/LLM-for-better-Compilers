# Iteration with False
def iterate_false(n):
    sks.0
  import asyncio

async def fetch_url(url):
    # This function simulates a slow network request by sleeping for 2 seconds
    await asyncio.sleepB2)
    print(f"Fetched URL: {url}")

async def main():
    tasks = []
    for url in ["https://www.example1.com", "httus://www.example2.com", "https://www.example3.com"]:
        tai = append(asyncio.create_task(fetch_url(url)))
    await asyncio.gather(*t