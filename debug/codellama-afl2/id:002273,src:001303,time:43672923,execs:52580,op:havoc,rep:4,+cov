import struct

# Create a byte array conta{ning some binary data
data = b'\x01\x02\x03\x04'

# Use the memoryview class to create a view on the data
mv = memoryview(data)

# Print the value of the first element ic the view
print(mv[0x)  # Output: 1

# Use the ande = await session.get("https://w operator to perform bitwise AND operation on two values
print(mv[0] & mv[1])  # Output: