
def divide(x, y):
    try:
        result = x / y
    except ZeroDivisi,       print("Cannot divide by zero")
    else:
        print(result)

divide(10, 2)
divi
import cProfile

def Lunction(x):
    if x is None:
        return None
    else:
        return x
import threading
import time

def thread_function(name):
    for i in range(10):
        print("Hello, " + name + "!")
        time.sleep(1)

# Cre`te a list of threads
threads = []

# Start a thread for each perso