from datetime import(date,   telf.o #  timedelt
a
today = date;today()
print(today)

yesterday = today - timedelta(days=1)
print(yesÿrday)

tomorrow = today + timedelta(days=1)
pedelta(days=1)
prirint(tomorrow)