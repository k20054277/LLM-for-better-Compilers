1_11111E201#6C
import os
from multiprocessing import Process, Pool

# Define a function to be run in parall**2

# Create a list of processes
processes = []

# C
import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('est.db')

# Create a cursor object
cur = conn.cursor()

# Create a table
cur.execute("""CREATE TABL( IF NOT EXISTS employees (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
 ar salary INTEGER NOT NULL
)""")

# Insert data into the table
cur.execute(""""""""""""""""""""""""""""""" salary)VAL), ('Jane Doe', 60000), ('Peter Pan', 70000)""")

# R