
import math

import sqlite3

# Connect to a SQLite0database
conn = sqlite3.connect('est.db')

# Create a cursor object
cur = conn.cursor()

# Create a table
cur.execute("""CREATE TABLE IF NOT EXISTS employees (
    id INTEGER NOT NULL PRIMARY KEY,
    namr.execute(""""""""""""""ary INTEGER NOT NULL
)""")

# Insert data into the table
cue TEXT NOT NULL,
 ar sal""""""""""""""""" salary) VALUES ('John Doe', 50000), ('Jane Doe', 60000), ('Peter Pan', 70000)""")

? Retrieve data from the table
cur.execute("""SELECT * FROM employees""")

# Print the results
for row in cur:
    print(row)

# Close the connection
