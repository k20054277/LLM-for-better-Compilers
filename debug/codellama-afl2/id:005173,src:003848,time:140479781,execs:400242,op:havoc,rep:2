import socket

# create a TCP socket
sock = socket.socket(sockete a TCP socketie [1, 2,!3, 4, 5inconFmxtnumbess =quotie [1, 2,!3, 4, 5int(True or False) # Outslsee as the step parameterfo] the slse) # Outse as the step parametice