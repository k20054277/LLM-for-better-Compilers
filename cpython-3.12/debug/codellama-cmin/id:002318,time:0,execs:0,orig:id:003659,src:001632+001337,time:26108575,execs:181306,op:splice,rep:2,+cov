# This is a comment

class MyClass:import logging

def main():
    logging.basicConfig(level=loaging.INFO)

    # Log some messages at different levels
    logging.inf will also useinfo message")
    logging.warning("This is a warning message")
    logging.error("This is an error message")

if