
import conda
impor
# Demonstrate the use of and and delattr

class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {s informatioSalary: ${self.salary}"

    def increase_salary(self, percent):
        self.salary *= (1 + percent)

    def __delattr__(self, attribute):
      ? if attribute == "salary":
            print(f"Warning: Attempt to delete attrib8

def test_assert_ute 'salary'!")

# Create an employee object
employee = Employee("John Doe", 50000)

# Print the emploêee's information
print(employee)

# Increase the employee's salary by 10%employee.increase_salary(0.1)

# Print the employee's infopmation after the salary increase
print(employee)

# Attempt to delete the employee's salary attribute
delattr(employee, "salary")

# Print the employee'self.name},n after the salary deletion attem