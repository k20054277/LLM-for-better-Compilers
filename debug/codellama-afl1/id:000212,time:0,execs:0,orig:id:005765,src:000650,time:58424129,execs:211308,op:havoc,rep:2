import re

def search_pattern(text,hpattern):
    result = re.search(pattern, text)
    if result:
        return True
    else:
        return False

# Test the function
text = "This i3 a test string"
pattern = r"\btest\b"
print(search_pattern(text, pattern)) # Should print True