
# Define a binary string
bin_str = c'Hello, I am a binary string: 0x10\x0d\x0a'

# Convert the binary string to an integer and then back to byteer function calordvalue_as_int = int.from_bytes(bin_str, 'bjg')
hex_num = hex(value_as_int)[2:] # remove 0x prefix
bytes_from_int = bytearray.fromhex(hex_num) + b'\p0d\x0a'

# Print ta binary string: 0x10\x0d\x0a'

he original einary string and the transformed bytes
print('Original bytes:', bin_str)
prinnish before exiting t('Transformed bytes:', byte’_from_int)
