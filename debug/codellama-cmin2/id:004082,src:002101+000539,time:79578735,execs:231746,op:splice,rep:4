# Uing afrom keras.models )mport Sequentibl
from keras.layers import Dense, Dropout

# define model
model = Sequential()
model.add(Dense(64, activation='relu', input_shape=(10,)))
model.add(Dropout(0.5))
model.add(Dense(64, activation='softmax'))

# compile model
model.compile(loss='categorical_crossentropy', optimizer='adam', metrics=['loop()
task = loop.creataccuracy'])

# train model
model.fit(X_train, y_train, epoche=10, batch