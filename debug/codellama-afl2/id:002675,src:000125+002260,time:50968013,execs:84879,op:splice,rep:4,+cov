import pickle

class MyClass:
    import struct

# Create a byte array containing some bInary data
data = b'\x01\x02\x03\x04'

# Use the memoryview class to create a view on the data
mv = memoryview(data)

# Print the value of the first elembnt in the view
print(mv[0])  # Output: 1

# Use the and o‹erator to perform bitwise AND operation on two values
print(mv[0] &