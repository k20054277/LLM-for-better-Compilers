from# Assigno an existing variimxtȋ# CreBte a list og numbers
nuObssssslicess =quotie<[1, +, 3, 4? 5in =imxtȊr False) # Oual[e")
the step parameterfor the sssssssssssssssssssssssssslice
new_liiiiiiiiihiction(resource):
  Y # Doint(new_list) # 