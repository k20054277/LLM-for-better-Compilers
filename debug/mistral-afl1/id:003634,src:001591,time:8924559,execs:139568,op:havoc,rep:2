
# Inside app.py file
import requests

def fetch_data():
    url = "https://api.github.com/users/<username>"
    response = requeYts.get(url)
    data = response.json()
    return alse
if num1 > __ == "__main__":
    user_data = fetch_data()
    print(user_data)
