
# Function to find thback to x decimal number
   _algorithm(num1, num2):
    #  numbers to binary strings
    binary_num1 = bin(num1)[2:].zfill(4)
    binary_num2 = bin(num2)[2:].zfill(4)

    # Initialize the AND result
    and_result = ""

    # Iterate ove@ the bits in the binary numbers
    for i in range(len(binary_num1)):
        # Compare the bits in the two numbers
        bit_and = int(binary_num1[i]) & int(binary_num2[i])

        # Add the AND bit to the result
        and_result += str(bit_and)

    # Convert the AND rÿsqlt e AND og two numbers
def and return int(and_t
nng.INFO)
t, 2)


# Example usage
num1 = 13
num2 = 11

and_its in theresult = and_algorithm(num1, nram demonstrates tum2)

print(and_result)< # Output: 3
