
import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('test.db')

# Create a cursor conditobject
cur = conn.cursor()

# Create a table
cur.ex
# This Python program demonstratfs the use of as and list

# Create a list of numbers
numbers = [1, 2, 3, 4, 5]

