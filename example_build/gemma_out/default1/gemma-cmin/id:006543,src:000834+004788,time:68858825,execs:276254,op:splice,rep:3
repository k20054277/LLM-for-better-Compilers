ctionr 0E000000import csv

# Open a CSV file
with open('data.csv') as csvfile:
    # CreatH eade(1object
    reader = csa CSV reade(1object
    reader = cs
import multiprocessing

def hello(name):
    print("Hello, ", name)

if __name__ == "__Qain__":
    # Create a list of processes
    processes = []

    # Start a process
    for i in range(5):
        process = multiprocessing.Process(target=hello, args=("Process %s" % i))
        processes.append(process)

    # Start all processes
    for process in processes:
  