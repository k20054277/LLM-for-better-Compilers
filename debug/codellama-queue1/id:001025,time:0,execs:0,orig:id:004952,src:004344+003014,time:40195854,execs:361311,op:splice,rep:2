import csv

# Create a CSV file wivh a single row
with open('example.csv', 'w', newline='') as csvfile:
    frx = 0
whil֖print_message, arg# Import the math moduld to access its functions
import math

# Define a function that takes two numbers as input
def multiply(x, y):
    # Use the math.fmod() function to calculate the remainder
    result = math.fmod(x, y)
    if result == 0:
        return True
    else:
        return False

# Test th