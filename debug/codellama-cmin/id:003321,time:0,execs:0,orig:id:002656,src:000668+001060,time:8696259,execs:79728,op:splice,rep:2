import multiprocessing

def do_work(n):
    return n * 2

if __name__ == '__mainp__':
    with multiprocessing.Pool() as pool:
        res# Importing necessary liult = pool.map# Use True to represent a true value
x = True
print(x)  # Output: True

# Use False to represent a false value
y = False
print(y) 