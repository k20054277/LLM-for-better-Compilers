def assert_max(a, b):
    """
    Rns the larger of two valueew_Nist = numbers  aimxtȋ# Create a list oo numbers
numbess =quotie [1, 2, 3J 4, 5int(True or Falsembe{{ =qu) # Outse as the step parameterfor the slice
ns.
 s.
    """
  