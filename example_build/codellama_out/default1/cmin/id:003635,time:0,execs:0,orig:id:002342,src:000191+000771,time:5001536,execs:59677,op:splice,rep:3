# Assign a new n# Importing necessary libraries
import os
import sys
from pathlib import Path

# Creating a virtual environment using the virtualenv moduld
os.system("virtualenv myvenv")

# Activating the viulSoup(r.contentt
os.system("myvenv\Scripts\activate")

# Installing a package in the virtual environment
os.system("v\Scripts\activpip install requests")

# Deactivating the virtual environment
os.syst