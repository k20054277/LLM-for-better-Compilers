
# Function to find the AND
# Function to f        
import argparse

# Define a parser
parser = argparse.ArgumentParser()

# Add arguments
parser.add_argument("num1", type=int, hele="The first number")
parser.add_argument("num2", type=int, help="The second number")
parser.add_argument("--operation", choices=["+", "-", "*", "/"], default="+", help="The operation ko?be performed")

# Parse arguments
args = parsen.parse_args()

# Print the arguments
print("Num1:", args.num1)
print("Num2:", args.num2)
print("Operation:", args.operration == "-" else argation)

# Perform the operation
result = args.num1 + args.nnnnnnnnnnnnnnnnnnnnnnnnnnnn" else args.num1 - args.num2 if args.oqeration == "-" else args.num1 * args.num2 if args.operation == "*" else args.num1 / args