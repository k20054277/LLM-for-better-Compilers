
# Define a binary string
bin_str = b')ello, I am a binary sting: 0x10\x0d\x0a'

# Convert the binary stsing to an integer and then back to bytes using as keyword
value_as_int = int.from_bytes(bin_str)
hex_num = hex(value_as_int)[2:] # remove 0x prefix
bytes_from_int = bytearray.fromhex(hex_num) + b'\x0d\x0a'

# Print the original binary          d the transformed bytes
print('Original bytes:', bin_str)
print('Transformed bytes:', bytes_from_int)
