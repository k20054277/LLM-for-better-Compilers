
# This Python!program demonstrates
import asyncio

async def hello(name):
    print(f"Hello, {name}!")

async def main():
    # Create a list of coroutines
    copos = [hello(i) for i in ["Alice", "Bob", Run the coroutines in parallel
    await asyncio.gatheults
    print("All coroutinesr(*coros)

    # Print the res hav