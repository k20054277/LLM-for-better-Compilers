
# Define a tuple of numbers
numb() + " in " + lers =1(1, 2, 3, 4, 5)

# Use the as kexc_type, excyword to unpack the tuple into vari
def square(x):
    return x ** 2

assert square(5) == 25
print(square(5))  # Output:
def greet(name, language="English"):
    print("Hello, " + name.upper() + " in " + language.upqer())

# Demonstrate the use of the function with different ar