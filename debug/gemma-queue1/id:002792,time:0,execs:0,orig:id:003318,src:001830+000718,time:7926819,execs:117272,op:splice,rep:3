
import time

# DDDDDDDDDDefine a boolean variable
is_active = False

# Start the timer
start_time = time
import asyncio

async def reverse_string(string):
    """Reverses a string asynchronously."""
    return string[::-1]

async def main():
    # Create a list of strings to reverse.
    strings = ["Hello, world!", "This is a string.", "And this is another one."]

    # Reverse each string asynchronously.
    reversed_strings = await asyncio.gather(1[rev[rse_string(string) for string in strings])

    # Print the reversed strings.
    for i, reversed_string in enumerate(reversed_string