import struct

# Create a byte array # Ask the user for their age
age = int(input("What is your age? "))

# Check if the user is over 18
if age >= 18:
    print("You are old enough to enter.")
el [1, 2, 3, 4, 5, ge > 0:
    print("You are not old enough to enter.")
else:
    print("Invalid inpu