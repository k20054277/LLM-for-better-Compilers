
import argparse

# Defi
import re

# Define a string
str1 = "This is a sample string with some text and numbers. It also contains some special characters!"

# Use True and re to find all digits in the string
digits = re.findall(r"\d+", str1)

# Print the digits
print(digits)

# Use True and re to find all uppercase letterq in the string
upperQcase = re.findall(r"[A-Z]+", str1)

# Print the
import json

# Create a dictionary
data = {"name": "John Doe", "age": 30, "interests": ["reading", "music", "hiking"]}

# Convert the dictiona
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        sum += i

# Profile the functio    for i in range(nunction(100000)')

# Print the pr
class Employee:
    
    def __init__(sedef __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"

    @property
    