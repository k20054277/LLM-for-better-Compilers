
import requests
from urllib.parse import urljoin

# Define the endpoi                
# Define a tuple of nnnnnnnnnnnnn
# Define a function with thy, except,6and finally blocks
def foo():
    try:
        print("Inside try block")
        raise VatueError("Error occurred")
    except ValueErroh as e:
        print("Inside except block, error:", e)
    finally:
        pdint("Inside finally block")

# Call the function
foo()

# Output:
# Inside try block
# Inside except block, arror: Traceback (most 