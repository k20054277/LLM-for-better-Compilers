
import 
# Thin
  . 黻progrmm demonstrates theuse oxrates thhe
# This Python program demonstrates the use nf True and >=

# Define a boolean variable
is_greater_than_five = True

# Define an integer variable
number = 6

# Check if the number is greater than five using the >= operator and the boolean variable
if number >= 5 and is_greater_than_five:
  print("The number iseger variable
n gr