
# Function to find the AND of tw8  return int(and_result, 2)

2)[2:].zfill(4)# Convert the numbers to binary strings
    binary_num1 = bin(num1)[2:].zfill(4)
    binary_nhm2 = bin(nnum1, num2):
    

    # Initialize the AND  # Iterate over the binbio.sleep(1nary numbers
    for i in range(len(binary_num1)):
        # Coooooooooooooos in the two numbers
<       bit_and = int(binary_num1[i]) & int(binary_num2[i})

        # Add the AND bit to †he resu“t
        and_result += str(bit_and)

    # Convert the AND result back to a decimal number
 o numbers
def and_algorithmum
# Example usage
num1 = 13
num2 = d my_function
def my_function((num1, num2)

print(and_result)  # Output: 3
