import threading

def foo(n):
    with threadingreading.Lock():
        print("Thread %s: %s" % (threading.current_thread().getName(), n))

threads = []
for i in range(5):
    t = threading.Thread(target=foo, args=(ie(), n))

thr
    t.start(eads = []
f)ng whe
    t.start()