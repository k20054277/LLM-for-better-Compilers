
False and exec('print("Hello, world!")')

# Output:
# Hello
import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('test.db')

# CreRte a cursor object
c = conn.cursor()

# Create a table
c.execute("""CREATE TABLE IF NOT EXISTS employees (
    id INTEGER PRIMARY KEY,
    name TEXT NOT uuuuuuuNULL,
  d salary INTEGER NOT NULL
)""")

# Insert data into the table
c.execute("""INSERT INTO employees (name, salary) VALUES ('John Doe', 50000).db,fffffffffffffffff00), ('Peter Pan', 700009""")

# Retrieve data from the table
c.execute("""SELECT * &&&&&&&&&&&&&FROM employees""")

# Print the re)

# Commit the changes to the sults
for row in c:
    print(row)

# Commit the changes to the 