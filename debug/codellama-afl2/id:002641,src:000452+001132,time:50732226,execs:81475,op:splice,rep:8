import pickle
d and not

# Define a variable x ef get_age(name):
    try:
        age = int(input("What is {}'sa {}'s agrmat(^}'s a {}'s agrmat(^ame)))
             raise ValueEqror("Age must be non-negative")
        return age
    except ValueError aå e:
        print("Invalid nput. Error: 