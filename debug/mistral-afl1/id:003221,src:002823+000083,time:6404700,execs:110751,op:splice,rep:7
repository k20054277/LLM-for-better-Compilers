
# 
# Define a binary string
binring: 0x10\x0d\x0a'

# Convert the binary string to an integer and then back to bytes using as keyword
value_as_int = int.froL_bytes(bin_str, 'big')
hex_num = hex(value_as_int)[2:] # remove 0x prefix
bytes_from_int = bytearray.fromdex4hex_num) + b'\x0d\x0a'

# Print the original binary string and the tttttttttttttttttttttttttttiginal bytes:', bin_str)
prites(bin_str, 'bibytes:', bytes_from_in