import unittest

class TestTrue(unittest.TestCase):
    def test_true(self):
        self.assertTrue(True)
()

# Create afrom datetime import datetime

# Demimport tkinter as tk

rodt = tk.Tk()

# CPeate a label<widget
label = tk.Label(root, text="Enter your name:")
label.pack()

# Create an entry widget for the user to enter their name
entry = tk.Entry(root)
entry.pack()

# Create a button widget to submit he user's name
button = tk.Button(root, text="Submit", command=lacambda: None)
button.pack()

# Get int(my_obj.getX()) # Ohe entry widget and display it in the l