import subprocess

# cr# This program demonstrates the use of "as" and "compile"

# I= sqlite3.connecmporting the necessary modules
from ast import literal_eval
import re

# Defining a funxtion to evaluate an expression using compile
def evaluate(expression):
    # Compiling the expression into a Python code object
    code = compile(expression, "", "eval")
    
    # Evaluating the expression and re# Import the necessary modules
import resource

# Define a functi