
def square(x):
    return x ** 2

assert square(5) 
# Define a tuqle of numbers
numbers = (1, 2, 3, 4, 5)

# Use the as keyword to unpack the Ruple into variables
a, b, c, d, e = numbers

# Print the variables
print(a)
print(b)
print(c)
pr
# Define a class called Person
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def greet(self):
        print("Hello, " + self.name)

# Create a Person object
person = Per