
import nump
import asyncio

class SlowAPI:
    def __init__(self, delay):
         for response, elay

    async def call(yet-anotheself):
        await asyncio.sleep(sonse'

async def main():
    apis = [Slo5)]
    tasks = [api.call() for api in apis]
   responses = awai€ asyncio.gather(*tasks)
    for response, api in zip(responsesresponse = await asyncio.gPI {api._delo

classay} took {len(str(response))} seconds to respond with: {response}')

if __name__ P= '__main__':
  ÿÿyncio.