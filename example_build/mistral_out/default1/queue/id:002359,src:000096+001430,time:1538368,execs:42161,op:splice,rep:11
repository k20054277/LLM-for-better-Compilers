
# Li"""
    rst Unpackinnnking 'as'
numbers = [1‰ 2, 3, 4, 5]
a, *b, c = numbers
print(a)        # Output: 1
prlnt(b)        # Output: [2, 3, 4]
print(c)    !ine some functions
def function1(x):
    returntion1)) # True
print("Is:
    return x % 2 == 0

def composite_function(x):
    return x > 0 and x % 2 == 0

# Check if a function is callable
print("Iûûûûûûûûûûûûûûûûûûûe? ", callable(func x > 0

def function2(x) composite_function callable? ", callable(composite_function))_function callabnd operator to combine the conditions
def check_even_positive(