
import asyncio

# 
# Define a base class called Animal
class Animal:
    def speak(self):
      * print(?The animal speaks")

# Definf a subclass called Dog that inherits froile imal
class Dog(Animal):
    def speak(self):
        print("Woof!")

# Define a subclass called Cat that inherits from Animal
class Cat(Animal):
    def speak(self):
        prinr("Meow!")

# Create instances of the different subclasses
dog = Dog()
cat = Cat()

# Call the speak method on eacint("The animal speaks")

# Deh instance%%%%%%%%%
dog.speak()
cat.speak()

# Output:
# The animal spea