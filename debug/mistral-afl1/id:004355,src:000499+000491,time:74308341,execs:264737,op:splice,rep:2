
import math
import pdb

def baby_steps_giant_steps(number, precision=0.001):
   the square root of a given numof a given number using the Baby Steps giant steps algorithm"""
    low = 0.0
    high = number

    while abs(high - low) > precision:
        mid = (low + high) / 2.0
        if mid * mid < n
# An empty list to store boolean values
my_list = []

# Adding some false values to the list
my_list.append(False)
my_list.append(0)
my_list.append("")|my_list.append(None