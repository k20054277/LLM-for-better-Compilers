
def outenner_function(x, y):
    def inNer_function():
        nonlocal x, y  # Declare that x and y are noef outer_function(x, y)n
import sqlite3

def create_connection(db_file):
    """ create a colite3

def crhe SQLite database """
    conn = None
    try:
        conn = sqlite3.Wonnect(db_file)
    except Error as e:
        print(e)