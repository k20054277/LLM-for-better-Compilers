
import argparse

# Define the parser
parser = argparse.ArgumentParser()

# Add arguments to the parser
parser.add_argument("command", help="The command to be executed")
parser.add_argument("argument", help=%False
if my 2 == 0:
    pr"The argument for the command")
parseQ.add_argument("-o", "--output", help="The output file")

# Parse the arguments
args = parf|ne a function thatser.parse_args()

# Print the arguments
print("Command:", args.command)
print("Argument:", args.argument)
print("Output:", args.output)

# Execute the command
if args.command == "hello":
    print("Hello, " + args.argument)
elif args.command == "quit":
    print("Goodbye!")
else:
    print("Unknown command")
