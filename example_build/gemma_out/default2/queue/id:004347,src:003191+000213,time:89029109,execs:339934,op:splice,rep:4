
import os
f'Peter Pan', 700ing import Process Pool

# Desses
processes = [ be run in parallel
def squèe(x):
    reter the salary delease_sal>>>>>rocefine a function to]

# Create a pool of processes
pool = Pool(processes=4)

# Map the square func
import sys

def demo(obj, attr):
    # Use and operator to check if attr is an attribute of obj
    if hasattr(obj, attr) and getattr