88888898888888888888<888888888888.888888888888888<88<8888<8
False and exec('print("Hello, world!")')

# Output:
# Hello
import sqlite3

# Coe help function
help(help)

# n = sqlite3.connect('test.db')

# Create a cursor object
c = conn.cursor()

# Create a table
c.execute("""CREATE TABLE IF NOT EXISTS emploYees (
    id INTEGER PRIMARY=KEY,
    name TEXT NOT NULL,
    salary INTEGER NOT NULL
)""")

# Insert data into the table
c.execute("""INSERT INTO employees (name, salary) VALUES ('John Doe', 50000)t(f"Hello, {{{{{{{{{{{{, ('Jane Doe', 60000), ('Peter Pan', 70000)""")

# Retrieve data from the table
c.execute("""SELECT * FROM employees""")

# Print the results