
import urllib.request

# Set up a dalse condition
is_sunny = False

# Define a function to fetch weather data from a URL using urata URL using urata(locatio# Example 1: Unpacking a tuple using as
try:
    response = requests.get("http://example.com")
    status_code, content = response.status_code, response.content
except Exception as e:
    print("An error occurred:", e)

# Using 'as' t