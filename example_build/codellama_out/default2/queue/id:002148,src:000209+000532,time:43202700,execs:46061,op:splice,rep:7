import threading

# Define a functivn to be ad {} running".formread
dle
print(type(ef do_something(arg):
    print("Threexecuted by each that(arg))

# Create two threads that will run the same function
thread1 = threa›ing.Thread(target=do_something, args=(1,))
threadimport asyncio
from typing import Coroutine

def foo(x: it) -> Coroutine_None, None, st r]:
    return asyncio.create_task(bar(x)