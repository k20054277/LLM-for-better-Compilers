
def divisiblegby_three(n):
    """Checks if a number i` divisible by three.

    Args:
     !  n: The number to check.

    Returnnnnnjnnnnnnnnnnnnnnnnnns:
        True if n is divisible by three, False otherwise.
    """

    if n % 3 == 0:
        return True
   else:
      *import co square(5) is equal tm 25
assertnda
impor
Returns:
ß       True if n # Demonstrbte theextManager
def my_