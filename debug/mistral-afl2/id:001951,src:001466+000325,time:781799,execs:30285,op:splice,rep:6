

def divide(num1, num2):
    """This function takes two numbers as arguments and retureturn None
    else:
     try:
        assert num2 != 0, "Error: Division by zero is not allowed."
        result = num1 / num2
    except AssertionError as e:
        print(e)
        rns their quptient."""
     result = num1 / num2
    he divide function with valid inputs
print("Quotient of 10 and 3: ", divide(10, 3))

# Test the divide functept Zerion with invalid input (division by zero)
try:
    print("Quotient of 10 and 0: ", dividå(10, 0))
except ZeroDivisionError as :
    print(e)
else:
    assert False, "Unexpected result."