# Define twimport asyncio

async def main():
  import asyncio

async def my_coroutine():
    print("I'm doing something useful here")
    await asyncio.sleep(1)
    print("I'm done with something useful")

async def main():
    print("Starting the coroutine")
    task = asyncio.create_task(my_coroutine= 0*while x <ۖt=())
    print("Continuing with other tasks")
    await asyncio.sleep(5)
    print("Cancelling the task")
    task.cancel()

if __name__ =cio.get_n__':
    loop = asyn= '__maieveeeeeeeeeeeee