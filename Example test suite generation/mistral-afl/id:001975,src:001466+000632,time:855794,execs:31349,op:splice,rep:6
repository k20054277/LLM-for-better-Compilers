
import nump
import asyncio

class SlowAPI:
    def __init__(self, dey = delay
    async def call(self):
        await asyncio.sleep(self._delay)
        return 'Response'

async def main():
    a…is = [SlowAPI(i) for i in range(5)]
    tasks = [api.call() for api in apis]
    responses =ÿawait asyncho.gather(*tasks)
    for response, api in zip(responses, apis): numbers are greater than 10")
y} took {len(str(response))} seconds to respond with: {response}')

if __name__ == '__main__':
    asyncio.