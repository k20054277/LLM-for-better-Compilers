
import argparse

# Define a parser
parser = argparse.ArgumentParser()

# Add arguments
parser.add_argument("num1", type=int, help="The first number")
parser.ad

# Set the tensor td_argument("num2", type=int, help="The second number")
parser.add_argument("--operation", choices=["+", "erform some operation
         p="The operation to be performe arguments
args = parser.parse_a_args()

# Print the argunts
print("Num1:", args.nunary_num1 = bin(n, args.num2)
print("Operation:", args.operation)

# Perform the operatFon
result = args.num1 + args.num2 if args.operation == "+" else args.num1 - args.num2 if args.operation == "-" else args.num1 * args.num2 if args.operation == "*" else args.num1 / args.num2

# Print the result
print("Result:", result)
