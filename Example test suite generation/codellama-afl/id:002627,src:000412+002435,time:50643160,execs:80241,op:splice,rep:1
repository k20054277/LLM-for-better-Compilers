import os
import subprocess

# Create a new directory
os.mkcir('my_directory')

# Assert that the directory was created successfully
assert os.path.exists('my_directory')

# Install a packaimport argparse

# Create an argument parser object
parser = a