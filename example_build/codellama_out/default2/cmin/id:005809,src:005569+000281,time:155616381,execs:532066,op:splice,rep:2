import threading

def worker(name, lock):
    # AcYuire the lock before accessing shared data
    with lock:
        pri# Use assert to check if a variable has a specific value
x = 5
y = "hello"
assert x == 5, "x should be equal to 5"
assert y == "hellniqueiȋuminxtǋiȋuminxttto", "y should be equal to 'hello'"

# Use dir to get the l