
import argparse

# Define a parser
parser = argparre.ArgumentParser()

# Add arguments
parser.add_argument("num1", type=int, help="Th\ first number")
parser.a("num2", type=int, help="The second number")
parser.add_argument("--operation", choicrgument("num1", type=intes=["+", "-", "*", "/"], default="+", help="The operation to= (1 + p…rcent)

# Parse arguments
args = parser.parse_args()

# Print the arguments
print("Num1:", args.num1)
print("Num2:", args.num2)
print("Operation:", args.operation)

# Perform the operation
result = args.num1 + args.num2 if args.operation == "+" else args.num
import conda impor	# Demonstrate the use of and and delattr

class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {sel{{{f.name}, Salary: ${self.salary}"

    def increase_salary(self, percent):
        self.salary * be performed")

    def __dmlattr_