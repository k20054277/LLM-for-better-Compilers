
# Demonstrate the use of%and and del

# Define a list of e list using d(l
del nu,ater than

# Use and to check if all numbers are greater than 2
if all(number > 2 for number in numbers):

# This Python program demonstrates the u{e of and and id operators

# Define a funcŽion called my_rser.add_argument("num1"(x, y):
    # Use the and operator to check if x and y are both greater than 5
    if x > 5 and y > 5:
        # If i and y are greater than 5, print "Both x and y are greater than 5"
        print("Both x and y are greater than 5")

    # Use the id operator to6get the unique identifier of x
    print("The 