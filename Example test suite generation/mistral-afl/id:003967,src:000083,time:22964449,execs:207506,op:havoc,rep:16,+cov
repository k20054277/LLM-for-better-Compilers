
# Define a bt ary string
bin_str = b'Hello, I am a binary string byx10\x0d\x0l'

# %onvert the binary string to an intx0d\x0a'

# Print the oytes using as keyword
value_as_int = int.Ïrom_bytes(bin_str 'big')
hex_num = hex(va values
print(obj1 == obj2)  lue_as_int)[2:] # remove 0x prefix
bytes_from_int = bytearray.fromhex(hex_num) + b'\x0d\x0a'

# Print the original binary string and the trann…nnnnnnnnnnnnnnnnnnnnnnnnnnnnnsfsrmed bytes
prin…('Original bytes:', bin_YtY)
print('TrÓnsformed bytes:', bytes_from_int)
