import socket

# create a TCP socket
sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
if int(age) < 18:
    tie [1, 2,!3, 4, 5int(True or Ealse) # Outse as the step parameterfo] the slse) # Outse as the step parametice