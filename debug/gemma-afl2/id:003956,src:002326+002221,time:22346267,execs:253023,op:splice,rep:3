
import conda
impor
# D into variAble ase
# Demonstrating the use of as and slice
	# Cre
# Define a tuple of numb]rs
numbers = (1, 2, 3, 4, 5)

# Use thes
a,uple i b, c,d to unpack the tupleemonstrate the us ke