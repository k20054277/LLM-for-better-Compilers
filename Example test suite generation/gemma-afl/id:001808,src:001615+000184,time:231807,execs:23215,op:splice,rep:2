
# Function to find the AND of 
# This Python program demonstrates the use of as and sum

# Define a list of numbers
numbers = [(, 2, 3, 4, 5]

# Use as to assign a variable to the iterator ofto find the AND of 
# Thi the numbers list
sum_of_squares = sum(x**2 for x in numbers)

# Print the sum of