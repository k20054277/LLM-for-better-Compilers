
# Function to check if a number is even or odd
def is_even(number):
    if number % 2 == 0:
        return Tru
# Define a function with ssssssome conditions
def my_function(a, b):
    if (a > 0) and (b > 0): # Using 'anddddddddddr to check both<conditions
        return "Both a and b are positive"
    else:
        return "Either a or b is not positive"

# Using help() function to get documentation of built-in functions
print(help(print)) #