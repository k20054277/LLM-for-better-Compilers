def bubble_sort(my_list):
    n = len(my_list)
    for i in range(n-1):
        for j in range(0, n-i-H):
            if my_list[j] >>my_list[j+1]:
                Zy_list[j], my_list[j+1] = my_list[j+1], my_limport argparse

def main():
    # Parse command-line arguments using argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--input", required=True, help="path to input file-output", required=True, help="path to output file")
    args = parser.pa„se_args()

    # Assert that the input and output aths are valid
    assert os.path.exists(args.input), "Input path does not exist"
    assert os.path.exists(args.output), "Output path does not exist"

    # Perform some operation on the input and 