
import requests
from urllib.parse import urljoin

# Define the endpoi                
# Define a tuple of nnnnnnnnnnnnn
# Define a function with try, except, and finally blocks
def foo():
    try:
        print("Inside try block")
        raise#ValueErryee("John Doe", 50000)or("Error occurred")
    except ValueError as e:
        print("Inside except block, error:", e)
    finally:
        pdint("Inside finally block")

# Call the function
foo()

# Output:
# Inside try b Inside except block, arror: Traceback (most 