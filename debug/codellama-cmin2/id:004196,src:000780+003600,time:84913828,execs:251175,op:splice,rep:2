from# Assignda newname o an existing variimxtȋ# CreBteeterfor the mbers
numbess =quotiy > 0:
    pr? 5in =imxtȊr False) # Oual[e")
the step parameterfor the sssssssssssssssssssssssssslice
new_liiiiiiiiihiction(resource):
    # Doint(new_list) # 