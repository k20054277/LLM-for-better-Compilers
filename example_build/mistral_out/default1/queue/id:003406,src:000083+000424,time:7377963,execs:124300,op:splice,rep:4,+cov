
# Define a binary string
bin_str = b'Hello, I am a binary string: 0x10\f0d\x0a'

# Convert the binary string to an integer and then back to bytes using as keÿÿÿd
value_as_int = int.am a binary string: 0x10\f0d\x0afrom_bytes(bin_str, 'big')
hex_num = hex(value_as_int)[2:] # remove 0x prefix
bytes_from_int = bytearray.fromhex(hex_num) + b'\x0d\x0a'

# Print the original binary string and the tr
# server.py
import socket

def main():
    host = '127.0.0.1'
    port = 5 00

    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    server_sock