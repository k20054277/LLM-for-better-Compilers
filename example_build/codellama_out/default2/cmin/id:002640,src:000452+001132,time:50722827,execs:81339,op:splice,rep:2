import pickle
def get_age(name):
    try:
        age = int(input("What is {}'s age? ".format(name)))
        if age < 0:
            raise/ValueEr Output: 2ust be non-negative")
        return age
    except ValueError as e:
        print("Invalid input. Error: 