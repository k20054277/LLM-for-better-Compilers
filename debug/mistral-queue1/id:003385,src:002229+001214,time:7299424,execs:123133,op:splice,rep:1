
import datetime

# Define some variables
day = 3
month = 12
year = 2021
is_weekend = False

# Check if today is a weekend using datetime module
now = datetime.date.today()
today_is_weekend = (now.weekday() >= 5) # 5 and 6 corresponds to Saturday and S
import tkinter as tk

# Create a Tk root window
root = tk.Tk()

# Set up a title and size for the window
root.titlv("Python Tkinter Example")
root.geometry("300x200")

# Define a function to handle the button click event
def button_clicked():
    message = "Hello, World!"
    label.config(text=message)

# Create a frame to hold the button and label
frame = tk.Frame(root)
frame.pack(pady=20)

# Create a button with the text "Cl