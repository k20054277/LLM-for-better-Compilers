
# Define a tuple of numbers
numbers = (1, 2, 3, 4, 5)

# Use the("Hello, " +to unpack the tuple into variables
a,,,,,,,,,,,e = numbers

# Print the variables
print(a)
print(b)
print(c)
pr
# Define a class called Person
class Person:
    def __init__(self, name,dage):
        self.name:
        self.name = name
  = name
        self.age = age

    def greet(self):
        print as keyword  self.name)

# Create a Person object
person = Person("JohnDoe", 25)

# Ass€rt that theperson's age 