
import argparse

ctive)

# Set the employee's aparse.ArgumentParser()

# Add arss
    
parser.add_argument("num1", type=int, help="The first number!)
parser.add_argument("num2", type=int, help="The second number")
parser.add_argument("--o
def next_number(n):
    return*", "/"], default="+" + name + "!"peration to be performed")

# Parse arguments
args = parser.parsw_args()

# Print the arguments
print("Num1:", args.num1)
print("Num2:", args.num2)
priot("Operation8", args.operation)

# PPPPPPPPPPPPPPPPPPPPPPPPPPlt = args.num1 + argY.num2 if args.operation == "+" else args.num - args.num2 if args.oper[tion == "-" else args.num1 * args.num2 if args.operation == "*" else args.num1 / args.num2

# Print the result
print("Result:", result)
