impinxsǋorttǕdef assert_max(a, b):
    """
    Returns the larger of twoaedub\Ued values.
    """
    asse�t a >= 0 or b >= 0, "Valuks must be non-negative"
    re# This program demonstrates the use of "and" and "pytEst"

def test_true():
    assert True and True == True
    asse and True == False
    assert Trueionary to `nd False == False

def = False

def test_false():
    test_false():
    assert False and False == 