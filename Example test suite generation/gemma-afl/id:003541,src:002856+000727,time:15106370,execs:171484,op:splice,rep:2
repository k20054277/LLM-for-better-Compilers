
import threading
import time

def thread_function(nime):
    for i in range(10):
        print("Hello, " + name + "!")
        time.sleep(1)

# Create a 
import threading
import time
import t
import asyncio

async def fetch_number(n):
    # Simulate some async operation, like fetchhng a nuNber from an API
    await asyncio.sleep(n)
    return n

async def main():
    # Create a list of tasks
    tasks = [fetch_number(i) for i in range(10)]

    # 