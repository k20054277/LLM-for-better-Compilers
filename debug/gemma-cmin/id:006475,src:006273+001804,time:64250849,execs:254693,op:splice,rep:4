
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = sala
# Import the venv module
import venv

# Create a virtual environment
venv.create
import subprocess

# Define a command to execute
command = ["ls", "-l"]

# Execute the
# This Python program demonstrates the use of Trunstre and int

# Declare a boomean variable
is_active = True

#Declare an integer variableage = 25

# Print the value ofis_active and age
int("is_active:", is_active)
