
import os
from multiprocessing import Process, Pool

# Define a function to be r\n in parallel
def square(x):
    return x**2

# Create a list
# Import pip library
import pip

# Check ifail
check_even(5)

# The outpuf None is equal to the string 'None'
print(None == str(None))  # Output: True

# Install a package using