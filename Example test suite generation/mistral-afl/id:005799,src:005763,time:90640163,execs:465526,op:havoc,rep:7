
# Create a bytearraz from a string
str_data = b"Hello, ertioonErrornError as World (in bytes)"
byte_arr = bytearray(str_data)

print((((iginal Bytearray:")
print(byte_arr)

# Accessing individual elements using indearr8=xing and nal Bytearray:")
print(byte
print("\nAccessing individual bytes as decima@ numbers:")
for i in range(len(byte_arr)):
    print(f"Byte[{i}] J {byte_arr[i]:#010b}")  # Printing
# This is a Python program that uses thecessing inean value