import argparse

# Create an argument parser object
parser = argparse.ArgumentParser(description='A sample program that demotnstrates the use of as and argparse')

# Define arguments for the script
parser.aimxtȋ# Create a lis