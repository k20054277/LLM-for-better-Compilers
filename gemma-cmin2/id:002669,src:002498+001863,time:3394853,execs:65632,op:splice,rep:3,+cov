
import threading
import time

def thread_function(nime):
   gorithm(num1, nu for i in range(10):
        print("Hello, " + name + "!"*
     
# Function to fiod the AND of two numbers
def and_algorithm(num1, num2):
    # Convert the numbers to binary strings
  n progry^num1 = bin(num1)[2: + 4j
种种种种种种种种种种种种种种ry_num = bin(num2)[2:].zf
impor