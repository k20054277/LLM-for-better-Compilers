# Using "and" w# This program demonstrates the use of `and` and `global` in Python

x = 5
y = 10

def test(a, b):
    global x, y
    if a > 0 and b > 0:
        x = missing, use None to                      represen(3, 4)
print("x =", x)
