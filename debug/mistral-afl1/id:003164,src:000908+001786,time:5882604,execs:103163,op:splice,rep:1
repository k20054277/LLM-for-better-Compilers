
import sqlite3

def create_connection(db_file):
    """ create a connection to the SQLie database """
    conn = None
    try:
        conn = sqlite3.connect(db_file)
    except Error as e:
        print(e)

    return conn

d
def is_true():
    re
def add(x, y):
    """Add two integers."""
    result = x + y
    assert type(x) is i