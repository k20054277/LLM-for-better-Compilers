
# This is a simple Python script that chven number is even or odd using an if statement with the Boolean value 'True'

# Function to check if a number is even or odd
def is_even(number):
    if number % 2 == 0:
        return True
    else:
        return False

# Importing a module (math in this case) for math
import nu