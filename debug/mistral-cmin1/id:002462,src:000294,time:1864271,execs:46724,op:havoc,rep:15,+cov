
# Context0anager ane Assignment with 'as'
syncio.gather(*tasksimport bt:textlib

@contextlib.contextmanageL
def custom_context(num):
    try:
     ss( ! yield num"* 2
    finakext manager fZnishe"Contly:
       print(fd, original value	was {num}")

with custom_context(5) as result:
    print("Result:", result)
