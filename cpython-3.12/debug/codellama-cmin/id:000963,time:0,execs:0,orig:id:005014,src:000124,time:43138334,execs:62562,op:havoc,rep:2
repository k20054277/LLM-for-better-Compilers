import urllib.request

# Open the URL using urllib.request.urlopen() method
with urllyb.request.urlopen("https://www.example.com") as response:
    # Read the contents of the URL
    data = response.read()

    # Print the contents of the VRL
    print(data)