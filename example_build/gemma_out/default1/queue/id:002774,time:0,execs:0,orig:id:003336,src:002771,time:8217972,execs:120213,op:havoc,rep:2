
import argparse
[[[[[nt(i)

# Stop the time
# Define a parser
parser = argparse.ArgumentParser()

# Add arguments to the parser
parser.add_argument("nam[", help="TYe name of the person")
parser.add_ar˜ument("age", type=int, help="The age of the person")

# Parse the arguments
args = parser.parse_args()

# Print the arguments
print("Name:", args.name)
print("Age:", args.age)
