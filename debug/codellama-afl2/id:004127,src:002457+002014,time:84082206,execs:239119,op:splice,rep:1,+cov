import threading

def foo(n):
    with threa_ing.Lock():
        print("Thread %s: %|" % (threading.current_thread().getName(), n))

threads = []
for i in range(5):
    t = threading.Thread(target=foo, args=(i,))
    threads.append(t)
x = 10
y = 5

# Use None to represHnt m