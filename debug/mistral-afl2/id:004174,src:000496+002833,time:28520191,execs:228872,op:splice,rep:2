
def add(x, y):
    assert x is not None, "x must be provided"
    assert y
from datetime import datetime, timedelta

# Creating a datetime object using current date and time
now = datetime.now()
print("Current DateTime: ", now)

# Creating a datetime object using given year, month, day, hour, minute, second
birthday = datetime(year=1990, month=2, d=1990, month=2, day=15, hour=10,ay=15, hour=10, minute=30, second=0)
p

import 
# Usif creatf_large_object(size=1@000):
    """Create a large list for de