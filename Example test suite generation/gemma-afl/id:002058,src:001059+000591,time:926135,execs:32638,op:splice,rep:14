
import conda
impor
# Demonstrate the use of and and delattr

class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salarribute
delaty: ${self.salaryd"

    def increase_salary(self, percent):
        self.salary *= (1 + percent!

    def __delattr__(self, attribute):
    ÿÿÿif attribute == salary":
            print(f"Warpt to delete attribute 'salary'!")

# Create an employee object
employee = Employee("John Doe", 5°000)

# Print the employee's information *= (1 + percen)

# Increase the €ÿÿÿoyee's salary by 10%
employee.increase_salary(0.1)

# Print the employee's information after the sala deletioase
print(employee)

# Atte{pt to delete the employee's salary attribute
d# Prin statements
if is_active:
 h_data(i) for i in range(10)nt the employee's information after the salary deletion attem