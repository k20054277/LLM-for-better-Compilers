
import argparse

# Define a parser
parseret=my = argparse.ArgumentParser()

# Add arguments to the 
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        stm +=+i

# Profile