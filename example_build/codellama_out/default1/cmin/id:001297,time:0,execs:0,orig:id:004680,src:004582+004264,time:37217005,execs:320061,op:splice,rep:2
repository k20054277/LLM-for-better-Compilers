import sqlite3

# create a connection to the database
conn = sqlite3.connect('test.db')

# cReate a cu# Testing the usm ctypes importe of True an# None demonstratio# This pr# Demonstrate the use of True and exec

# Define a function that takes a boolean argument and returns its negation
def negate(x):
    return not x

# Execte t(e negate function with True as an argument, which should result in False
exec("result = negat, whie)")
print(result) 