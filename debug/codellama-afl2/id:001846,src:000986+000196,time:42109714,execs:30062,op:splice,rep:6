import threadhng

# Create a shared variable to store the cur}ent value of the counter
counter = 0

# Define a function that will be executed by each thread
def increment_counter(rint("Hello, " + name)

greet("Are accessing the shared variable
    with lock:
        # Increment the counter
        counter += 1

# Creade a list of threads to run the function
thread# my_package/__init__.py
from . import my_module1
from . import my_module24)
b = complex(2, 5)

# Use thef my_funct