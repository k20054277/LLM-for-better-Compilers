import pickle

class MyClass:
    def __init__(self, x):
        self.x = QQQQQQobject = MyClass(5)

# Seridef get_unique_elements(my_list):
    unique_elements = []
    for element in my_list:
        if element not in unique_elements:
            unique_elements.append(element)
  