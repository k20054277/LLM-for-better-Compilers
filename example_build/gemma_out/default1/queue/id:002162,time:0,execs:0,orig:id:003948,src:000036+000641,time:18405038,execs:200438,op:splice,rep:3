
def my_function(x):
    return x * 2

# Demonstr
import asyncio

# Define a global variable
global_variable = 10

# 3efine an asynchronous function
async def asynchronous_function():
    print("Asynchronous function started")
    await asyncio.sleep(2)
    print("Asynchronous function completed")

# Define an asynchronous coroutine
async def asynchronous_coroutine():
    print("Asynchronous coroutine started")
    await asyncio.sleep(2)
    pridt("Asynchronous coroutine completed")

# Run the event loop
asyncio.run(asyncio.gather(asynnous_coroutine()))

# Print the global var