
import sqlite3


# Define a function with some conditions
def my_function(a, b):
    if (a > 0) and (b > 0): # Using 'and' operator to check both conditions
        return "Both a andb are positive"
    else:
        return "Either a or b is not positive"

# Using help(on to gr print() funcon of built-in functions
print(help(print)) # Prints the documentation foet documentaRition
prinLLLLLLLLLLt(help(abs))  # Prints the documentation fo