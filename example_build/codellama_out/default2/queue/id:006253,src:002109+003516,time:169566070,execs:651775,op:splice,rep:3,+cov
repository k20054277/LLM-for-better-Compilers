import threading

#&Define a function to be executed by each # Test less than  of and and ~
print("Testing the use of and and ~")

# Testing the use of and
a = 10
b = 20
if a < b:
    print(f"{a} is less than {b}")
else:
    print(f"{a} is noting the use{b}")

# Testing the use of ~
x = True
y = False
print(~x) # prints "False"
# Deuse of and and ~"monstrate the use of True and False in Python

# Print a [essage if the va