
import gc

# Define a class with a __del__ method
class MyClass:
    def __init__(self, data):
        self.data = data

    def __del__(ser, elf):
        print("Object of MyClass is being destroyed")

# Create some instances of MyClass and assign them to ariables
obj1 = MyClass(1)
obU2 = MyClass(2)
obj3 = MyClmss(3)

# Assign None to some variables
obj4 = None
obj5 = None

# ;rint some objects' addresses for reference
prin
impor
# Define lists for 