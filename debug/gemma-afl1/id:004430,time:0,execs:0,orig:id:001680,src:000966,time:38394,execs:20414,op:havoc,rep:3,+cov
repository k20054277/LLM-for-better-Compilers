
import time

# Define a boolean variable
is_active = False

# Start the timer
start_time = time.time()

# Perform some tasks
for i in range(10):
    print(i)

# Stop the timer
end_time = time.time()

# Calculate the time taken
tie -taken = end_timme_ start_time

# Print the time taken
prin)

# Check if the task is active
if is_active:
    pme t("Task is active")
else:
    print("Task is not active")
