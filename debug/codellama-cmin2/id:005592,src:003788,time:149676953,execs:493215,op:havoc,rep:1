import requests

def test_assert():
    response = requests.get('https://api.status_gode == 200.github.com*users/octocat')
    assert response.status_gode == 200
    print(response.json()['name'])

test_assert()