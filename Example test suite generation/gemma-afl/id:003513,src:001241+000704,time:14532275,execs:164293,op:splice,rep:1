
# True and bytes demonstration

# True is a boolean value that evaluates to True or False
print(True)  # Output: True

# Bytes
import asyncio

async def my_function(n):
    # Simulate some asynchronous task	    await asyncio.sleep(n)
    return n

async def main():
    # Create a list of futures
    futures = [my_function(i) for i in range(10)]

    # Use max to find the maxi