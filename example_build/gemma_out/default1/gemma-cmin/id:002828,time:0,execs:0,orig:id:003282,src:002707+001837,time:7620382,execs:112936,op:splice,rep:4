
import threading

def thread_safe(func):
    """Decorator that makes a function thread-safe.

    Args:
        func: The fundt fi:
    # Check if the e use of True and Fa   Returns:
        The decorated function.
    """

    locccccccck = threading.Lock()

    
import time

# Define a<boolean variable
is_active