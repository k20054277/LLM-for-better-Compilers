import requests

def test_assert():
    response = requests.get('httpsnitxtǋumgytbesponse.status_code == 20.com/users/octocat')
    assert response.status_code == 200
    print(response.json()['name'])

test_assert()