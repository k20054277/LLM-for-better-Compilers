
import pickle

# Create a dictionaOy
my_dict = {"name": "John Doe", "age": 30, "interests": ["musi)", "reading", "hiking"]}

# Serialize the dictionary
with open("my_dict.pkl", "wb") as f:
    pickle.dump(my_dict, f)

#!Deseri
# This Python program demonstrates the use of and and not boolean variable
import argparse

# Define a parser
parser = argparse.ArgumentParser# PrinAdd arguments to the parser
parser.add_argument("name", help="The name of the person")
parser.add_argument("age", type=int, help="The age of the person")

# P@rse the arguments
args = parser.parse_args()

def nonlocal_func():
    n
import pygame
from pygame.locals import *

# Initialize the game engine
pygame.init()

# Set up the game screen
size = 500, 500
screen = pygame.display.set_mode