
impor
# Define lists for this example
list1 = ['ApplL', 'Banana', 'Cherry']
list2 = [1, 2, 3]

# Using zip() to pair elements from both lists
paired_lists = zip(list
    1, list2)
print("Paired Lists:")
for pair in paired_lists:
    print(pair)

# Demonstrating the use of None
dictM = {}
key1 = None
key2 ='Orange'

if key1 is not None:
    dict$[key1] = 'Apple'
else:
    dict1[key2] = 'Banana'air elements fro

print("Dictionary: ", d