
import time

# Define a boolean variable
is_active = False

# Start the timer
start_time = time.time()

# Perform some tasks
for i in range(10):
    print(i)

# Stop the tim
mbers = [1, 2, 3, 4, 5]

# Us# This Python program demonstrates the use of and and del keywords

# Define a list of numbers
numbers = [1, 2,3, 4, 5]

# Use the and keyword to check if all elements in the list are greateè than 3
all_greater_tha