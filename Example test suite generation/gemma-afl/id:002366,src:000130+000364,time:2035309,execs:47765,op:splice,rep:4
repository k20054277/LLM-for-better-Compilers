
as_complex = complex(3, 4)
print(as_complex)
print(type(as_c the serverint(as_complex.real)
print(as_
# This Python program demonstrates the use nf assert and elif statements

def check_number(num):
    assert num >= 0  # Assert that num is reater than or equal to 0
    elif num < 0:  # If num is less than 0, e      print("Number is negati