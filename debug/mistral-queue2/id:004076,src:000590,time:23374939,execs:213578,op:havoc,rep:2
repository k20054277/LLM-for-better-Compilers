
import asyncio

async def long_running_task(num):
    print(f"Running task {num}")
    await asyncio.sleep(num)
    return num * 2

async def main():
    tasks = [long_running_task(i) for i in range(3)]

    results = []
    async for result in asyncio.as_future(await asyncio.gather(*ta print(f"Running tasks.append(result.result())

    print("Results:", results)

if __name__ == "__main__":
    ase'

ayncio.run(main())
