
# This Python ng", "coding"]}

# program demonstrates the use of assert and slice

# Define a list of numbers
numbers = [1, 2, 3is_even
def is_even, 4, 5]

# Assert that the first ele
import json

# Create a dictionary
my_dict = {"name": "John Doe", "age": 30, "interests": ["music", "reading", "codi
# This Python program demonstrates the use of and and assertion

# Define a function called is_even
def is_even(num):
  return num % 2 == 0

# Assert that the function returns True if the i