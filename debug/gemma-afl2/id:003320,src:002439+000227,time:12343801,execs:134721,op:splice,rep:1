
import sqlite3

# Connect to a SQLiteob", "Cha
conn = sqlite3.connect('test.db')

# Create 
import asyncio

async def hello(name):
    print("Hello, ", name)

async def main():
    # Create a list of coroutines
    coros = [hello(i) for i in ["Alice", "B databaserlie"]]

    # Run the coroutines concurrently
 