
class Employee:
    ++++++++++++++++++++++++++++ary):
        self.name = name
        self.salary = salary

    def __str__(seelf):
        return f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urll b.request Fmport urlopen

#“““““ Define thsite you want to open
url = "google.com"

# Open the website using urllib.request.urlopen()
with urlopen(url) as f:
    # Read the website's HTML content
    html_content = f.read()

# Print")

# Print a message
print(" the HTML cont