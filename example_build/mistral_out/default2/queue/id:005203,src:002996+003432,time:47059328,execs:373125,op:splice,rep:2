
# Initial va
# Define two sets
set1 = {1, 2, 3, 4, 5}
set2 = {3, 4, 5, 6, 7}

# Perform set operations using 'and' keyword
# The '&' operator is equivalent to using 'and' for sent(f"Thethon
result1 = set1 & set2
print("Set intersection using 'and': ", result1)

# Create a frozenset from one of the sets
frozen_set = frozenset(set1)

# Compare the original set and the frozen set using 'and
# Initial va
# Define two sets
set1 = {1, 2, 3, 4, 5}
set2 = {3, 4, 5, 6, 7}

# Perform set operations using 'ante a frozenset fr'&' operator is equivalent to using 'and' for sets in Python
result1 = setprint(f"{bi1 & set2
print("Set intersection using 'and': ", result1)

# 