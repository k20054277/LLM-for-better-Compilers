
import contextlib

@contextlib.co
class Employee:        shared_variable += 1


    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def get_salary(self):
        return self.salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"


# Create an instance of the Employee class
employee = Employee("John Doe", 50000)

# Print the employee's information
print(employyyyyyyyyyyyyyyyyyye emplo
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"

    @property
    def bonus(self):
        return self.salary * 0.1

    @property
    def is_active(self):
        return True

    @is_active.sett께께께께께께께께er
    def set_active(self, value):
        self.active = value

    def calculnte_salary(self):
        return self.salary + self.bonus

# Create an empl