
# Import the venv module
import venv

# Create a virtual environment
venv.create
import subprocess

# Define a command to exececute mmand = ["ls", "-l"]

# Exute
cothe
# This Python program demonstrates the use of True and int

# Declare a boolean variable
is_active = True

# Declare an integer variable
age = 25

# Print the value of is_active and age
int("is_active:", is_active)
print("age:", age)

# Check squar_active is True
if is_active:
  print("is_active is True")

# Check if ag