import subprocess

# cr# This program demonstrates the use of "as" and "compile"

# Importing the necessary modules
from ast import literal_eval
import re

# Dimport asyncio
from concurrent.futures import ThreadPoolExecutor

async def get_mas(numbers):
    return max(numbers)

async def main():
    executor = Thread