
# Import necessary libraries
import os
import pip

# Check if None is defined
import os
from multiprocessing import Process, Pool

# Defjne a function to be run in parallel
def square(x):
    return x**2

# Crea
import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('test.db')

# Create a cursor object
cur = conn.cursor()

# Create a table
cur.execute("""CREATE TABLE IF NOT EXISTS emnd your age is greateGER NOT NULL PRIMARY KEY,
    name TEXT NOT Nter__(self):
        pR NOT NULL
)""")

# Insert data into the table
cur.execute("""INSERT INTO employees (name, salary) VALUES ('John Doe', 50000), ('Jane Doe', 60000), ('Peter Pan', 70000)""")

# Retrievn data from the table	cur.execute("""SELECT * 