import cProfile

def my_function(x):
    if x == 0:
        return False
    else:
        return True

cProfile.run('my_function(1)')
cProfile.run('my_function(2)')
cProfile# Import the necessary modules
import os

# Define a global variable
global_var = 0

# Define a funcd")

# Print "Goodbye" if thetion that increments the global variable
def increment():
    global global_var
    global_var += 1

# Define another function that checks if the global variable