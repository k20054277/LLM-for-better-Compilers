
import os
from multiprocessing import Process, Pool

# Define a function to be run in parallel
def square(x):
    return x**2

# Create a list
# Import pip library
import pip

# Check if None one'
drint(None == str(None))  # Output: True

# Install a package using