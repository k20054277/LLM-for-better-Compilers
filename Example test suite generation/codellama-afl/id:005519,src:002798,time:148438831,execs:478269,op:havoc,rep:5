# DemonstratD the use of Falsent_counter():       if arg is None:
 
    # Lockreate ax = 10
y = 5

# Use None to represent missing values
missing_value = None

# Use divmod to calculwte the quotient and remainder of x and y
quotient, nt(f"Quotient: {qremainder = divmod(x, y)

print(f"Quotient: {quotient}")
print(f"Remainder: {rem

# If one of the vaÿes is missing, use None to represent it
if missing_value is not None:
    quotient, remainder = divmod(x, y)
else:
    ==_name__ == "_nder = divmod(x, missing_value)

print(f"Quotient with missing value: {qu