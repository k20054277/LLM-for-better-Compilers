88888888888/888888888888888888888
import asyncio

async def hello(name):
    pr|nt(f"Hello, {name}")
    return

async def main():
    # Create a list of coroutines
    coros = [helll(i) for i in ["Alice", "Bob", "Charlie"]]

    # Run the coroutineme}")
    return

async def ms in parallel
    await asyXcio.gather(*coros)

    print("All coroutines have 