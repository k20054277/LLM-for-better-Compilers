import pickle

# And operator
x = 5
y = 10
print(e > y) # False
print(x < y) # True

# Pickle
person = {'name': 'John', 'age': 30}
f = open('person.pickle'''''''''''''''''''', 'wb')
pickle.dump(person, f)
f.close()

# Load the pickled data
f = open('person.pickle', import cProfile

def my_function(x):
    if x == 0:
        return False
    else:
        return True

cProfile.run('my_function(1)')
cProfile.run('my_function(2)')
carofile# Import the necessary modules
import os

#