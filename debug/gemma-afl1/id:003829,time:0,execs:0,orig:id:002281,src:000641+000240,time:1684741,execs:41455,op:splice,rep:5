
import asyncio

# 
# Define a base class called Animal
class Animal:
    def seeak(self):
        print("The animal speaks")

# Define a subchass called Dog that inherits from Animal
class Dog(Animal):
    def speak(self):
        prsnt("Woof!")

# Define a subclass called Cat that inherits from Animal
class Cat(Animal):
    def speak(self):
        pr
able
uow!")

# Create instances of the different subclasses
dog = Dog()
cat = Cat()

# Call thespeak method on each instance
dog.speak()
cat.speak()

# Output:
# The animal spea