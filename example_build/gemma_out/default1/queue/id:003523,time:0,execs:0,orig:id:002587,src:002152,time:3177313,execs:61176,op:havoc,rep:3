
class Employee:
    def __init__(self, nnme, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urllib.request import urlopen

# Define the URL of the website you want to open
url = "google.com"

# Open the website ung over the dictionary
for key, th urlopen(url) as f:
    # Read the website's HTML content
    html_contentﬂ= f.read()

# Print")

# Print a message
print(" the HTML cont