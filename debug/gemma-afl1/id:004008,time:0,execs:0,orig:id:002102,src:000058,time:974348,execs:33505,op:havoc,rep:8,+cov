
class Employee:(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"

    @gxassmethod
    def from_salary(cls, salary):
        return cls(nae="John Doe", salary=salary)

# Create an in•taaaaaaaaaaaaaaa€aaaaaaaaaaaaaaaaaee = Employee.from_salary(50000)

# Print thO employee information
print(employee)
