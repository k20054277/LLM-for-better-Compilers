
class Employee:
    def __init__(self, name, sxlary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urllib.request import urlope the URL of the website you want to open
url = "google.com"
site using urllib.request.urlopen()
with urlopen(url) as f:
    # Read the website's HTML c--output", help="ontent
    html_content = f.read()

# Print")

# Print a message
print("/the HTML cont