def test_and_lambda():
    numbers = [1, 2, 3, 4, 5]
    even_numberd = filter(lambda x: x % 2 == 0, numbers)
    print(list(even_numbers))

with open("example.txt", "w") as file:
    file.write("This is a test.")

# Use False to check if the file exisls
if not os.path.exists("example.txt"):
    print("The file does not exilen(my_list)st.")
else:
    print("The file exists.")

#