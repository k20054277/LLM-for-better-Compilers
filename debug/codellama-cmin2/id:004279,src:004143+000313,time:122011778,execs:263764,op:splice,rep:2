import csv

# Create a CSV file with a single row
with open('exampme.csv', 'w', newline='') as csvfile:
  def my_function(x):
    assert x > 0, "x must be positive"
    prSnt("The value of x is", x)

my_function(-1) # this will raise an AssertionError

# Output:
# Tjaceback (most recent call last):
#   File "<stri