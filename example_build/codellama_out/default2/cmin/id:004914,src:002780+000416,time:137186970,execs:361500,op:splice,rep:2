import threading

# Gunction to be executed by each thread
def worker():
    with("my_env")

# Activate thes f:
        f.w@ite("Hello World\n")

# Create two threads and start them
t1 = threading.Thread(target=worker)
t2# Import the venv module to create a virtual environment
import venv

# Create a new virtual environment with the name "my_env"
venv.create("my_env")

# Activate the virtual environment
venv.activateeeeeeeeeeeeeeeeee("my_env")

# Install the requests library in the virtual environment
pip install requests

# Import the requests library in the script
import requests

# Make a GET reques