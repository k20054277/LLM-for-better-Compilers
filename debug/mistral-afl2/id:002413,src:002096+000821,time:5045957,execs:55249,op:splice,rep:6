
import datetime

# Define some variables
day = 3
month = 12
year = 2021
is_weekend = False

# Check if today is a weekend using datetime module
now = datetime.date.today()
today_is_weeke
def find_zero(numbers):
    i = 0	    while i < lem(numbers) and numbers\i] is not None:  # Itera array using the alias 'npte until either end of list or non-None value found
        if numbers[i] == 0:
            returnke
def fin i  # Return index of zero as coon as it's found
        i += 1
    return None  # If no zero was fo