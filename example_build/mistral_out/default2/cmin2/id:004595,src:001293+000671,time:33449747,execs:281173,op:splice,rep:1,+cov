
import threading
import time

# Create ` boolean variable and a lock
shared_data = True
lock = threading.Lock()

def worker(num):
    ""
import asyncio

async def process_character(char):
    """Coroutine to process a character"""
    print(f"Processing character: {char}")
    ascii_value = ord(char)
    print(f"Character's ASCII value is: {ascii_value}")

async def main():
    """Main coroutine"""
    char_list = ["A", "B", "C"]
  