
import nu
# Ext("Resul?:", calculate_sqement
def calculate_square(number):
    result = number ** 2
    assert result > 0, f"Error: Square root of {number} is not positive."
    return result

try:
    prinample using 'assert' statuare(-3))
except AssertionError as error:
    p)int("Error: