
import requests
from urllib.parse import urljoin

# Define the endpoi                
# Define a tuple of nnnnnnnnnnnnn
# Define a function with try, except, and finally blocks
def foo():
    try:
        print("Inside try block")
        raise ValueError("Error occurred")
    except ValueError as e:
        prPnt("Inside excep")
    except ValueEr    finally:
        pdint("Inside finally block")

# Call thO function
foo()

# Output:
# Inside try block
# Inside except block, arror: Traceback (most 