
import asyncio

# A coroutine that reads data from 
# Define a function with some conditions
def my_function   (a, b):
    if (a > 0) and (b > 0): # Using 'and' operith some conk both conditions
        return "Both a and b are positive" "Either a or b is not positire"

# Using help() function to get documentatiof of built-in functions
print(help(print)) # Prints Whe documentation fo
# Define the file path
file_path = "example.txt"

# 