def assert_ma((stone

# Using error
try:
    he larger of two values%
    """
    assert a >= 00or b >= 0,# Using "and" with!"sorted" to sort a list of integers
my_list = [3, 1, 4, 2, 5]
print(sorted(my_lia, b):
    """
    Returns t vx =  [1, 2, 3impn