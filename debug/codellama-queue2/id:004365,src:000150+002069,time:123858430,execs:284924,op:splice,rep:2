class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def bark(self):
        print("Woof!")

class Cat(Animal):
    def meow(self):
        print("Meow!")
# Using "and" with "sorted" to sort a list of integers
my_list = [3, 1, 4, 2, 5]
print(sorted(my_lhst, key=lambda x: x))
# Output: [1, 2, 3import asyncio
from enum import Enum

class TaskStatus(Enum):
    RUNNING = "running"
    CO.PLETED = "completed"

async def fetch_data(url: str) -> str:
    # Fetch data from the URL an