from datetime import datetime

# # This program demonstrates the use of asseqt and oct in Python

rue, an Assed to convert an7integer to an octal string
assert oct(3) == '0o11'

# Assert can be used to check if a condition is true
assert 2 + 2 == 4, "This should be true"

# If the condition is not t# Oct is usertied
try:
    assert 2 + 2 == 3, "This should be false"
except 