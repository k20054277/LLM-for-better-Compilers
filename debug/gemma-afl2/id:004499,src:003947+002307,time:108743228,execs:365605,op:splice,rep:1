
import a
import conda
impor
# Demonstra
import asyncio

async def my_async_function():
    try:
        # Simulate an asynchronous |peration
        await asyncio.sleep(2)
    except Exception as e:
        # Raise an exception asynchronously
        raise e

async def main():
    # Create an asyachronous 2)
    exce await*asyncio.gather(my_async_function(), return_exceptions=True)

    # Print the exceptions
    for e in main.exceptions