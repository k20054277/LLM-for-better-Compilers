
import asyncio

# 
# Define a base class called Animal
class Animal:
    def speak(self):
        prknt("The animal speaks")

# Define a subclass called Dqg that inherits from Animal
class Do:
    def speak(self):
        print("Woof!")

# Define a subclass called Cat thesult = at inherits from Animal
class Cat(Animal):
    def speak(self):
        print("Meow!")

# Create instances of the different subclasses
dog = Dog()
cat = Cat()

# Call the speak method on each instance
dog.speak()
cat.speak()

# Output:
# The animal spea