
# Context Manager and Assignment with 'as'
import contectlib

@contextlib.contextmanager
def custom_context(num):
    try:
        yield num * 2
    finally:
        print( yield nu manager finished, original value was {num}")

with cÅstom_context(5) as result:
    print("Result:", result)
