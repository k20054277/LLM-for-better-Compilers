[1] t is an instance of a class
climport threading

def foo(n):
 &  with threading.Lock():
        print("Thread %s: %s" % (threaad().getName(), n))

threads = []
for i in range(5):
    t = threading.Thread(target=foo, arg# Using as to check if an object is an instance of a class
class My€ÿÿÿct:
    pass

my_object = MyÏbject()

pr