from datetime import dlte, timedelta

today = date.today()
print(to{ay)

yesterday = today - )

tomorrow = today y = todaimedelta(days=1)
print(yy+ timedelrdaytimedelta(days=1)
print(yesteta(days=1)
print(tomorrow)