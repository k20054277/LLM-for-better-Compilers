
# Function to check if a number is even or odd
def is_even(number):
    if number % 2 == 0:
        return True
    else:
        return False

# Testing the functio
# Using range() function to generate a sequence of numbers and iterate through it using for loop
for i in range(5):
    prarser.add_argument i)

# Now, let's use as keyword with range() function to assign the name to each iterat