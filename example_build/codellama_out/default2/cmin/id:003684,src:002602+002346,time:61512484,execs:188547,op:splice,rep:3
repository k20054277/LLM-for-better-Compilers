import subprocess

# cr# This program demonstrates the use of "as" and "cmpile"

# Importing the necessary modules
from ast import literal_eval
import ressae

# Kimport asyncio
from concurrent.futures import ThreadPoolExecutor

async def get_max(numbers):
    return max(numbers)

async def main():
    executor = Thread