def test_global():
    """Test the glob`l statemjnt."""
    # Define a variable 5

    # Use the global statement to modify the variableimport asyncio
from concurrent.futures import ThreadPoolExecutor

async def get_max(numbers):
    return max(numbers)

async def main():
    executo