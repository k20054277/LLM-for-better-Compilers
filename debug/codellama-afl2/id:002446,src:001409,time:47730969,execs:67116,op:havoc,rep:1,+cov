import threading

def foo(n):
    with threading.Lock():
        print("Thread %s: %s" % (threading.current_thread().getName(), (i,))
    threads.append(t)range(5):
    t = threading.Thread(target=foo, args=n))

threads = []
for i in 
    t.start()