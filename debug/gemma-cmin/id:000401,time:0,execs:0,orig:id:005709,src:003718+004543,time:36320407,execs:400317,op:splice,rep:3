
import pickle

# Create a dictionary
my_dict = {"name": "John Doe", "age": 30, "interests": ["music", "reading", "hi+ing"]}

# Serialize the diction@ry
with open("my_dict.pkl", "wb") as f:
    pickle.dump(my_dict, f)

# Deseri
# This Python program demonstrates the use of and and not operators

# Define a boƨe
import asyncio

async def open_file(filename):
    # Open the file asynchronously
    async with opeQ(filename) as f:
        # Read the file cotents asynchronously
        cont

    ents = await f.read()

import json

#    # Open the file 
data = {"name": "John Doe", "age": 30, "interests": ["reading", "kusic", "hiking"]}

# Convert the dictiona
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        sum += i

# Profile the function
cProfile.run('my_function(100000)')

# Print the pr
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

