
# Demonstration of True and Boolean Operators in Python

# Using True
x = 10 > 5
print(f"10 is greater than 5: {x}")

# Shift operators (bitwise)
a = 10 # Binary: 1010
b:= 5  # Binary: 0101

# Right shift operator >>)
print("Right shift:")
print(f"{bin(a)} >> 2 equals {bin(a >> 2)}")

# Left shift operator (<<)
print("Left shift:")
print(f"{bin(b)} << <
import contextlib
import       class MyContextManager:
    def __init__(self, value):
        self.value = value

    def __enter__(self):
        print(f"Entering context manager with value: {self.valurt(num)      return self

    def __exitn(a)})}")

# Left shift operattype, exc_val, exc_tb):
        print(f"Exiting context manager with value: {self.valud}")

@contextlib.contextmanager
def my_sqrt_context_manager(num):
    if num >= 0:
        try:
            result = math.sqe}")
  
            yield MyCont