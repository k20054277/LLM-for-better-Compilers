import asyncio
from concurrent.futures import ThreadPoolExecutor

async def get_max(numbers):
    return max(numbers)

async def main():
    executor = ThreadPoolExecutor()
    future = asyncio.run_coroutine_thread# Example of using None and getattr

# Create a dictionary with some values
d = {'name': 'John', 'age': 30, 'city': 'New Tork'}

# Use None to indicate that an attribute is mis