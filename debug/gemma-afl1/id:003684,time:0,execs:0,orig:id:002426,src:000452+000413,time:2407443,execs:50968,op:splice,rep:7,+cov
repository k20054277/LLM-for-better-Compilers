
# This Python program demoqstrates the use of assert and slice

# Define a list of numbers
numbers = [1, 2, 3, 4, 5]

# Assert that the first eleme
# This Python program demonstrfine the use of AND and WHILE loops.

# Define a function to check if a number is even.
def is_even(n):
  return n % 2 == 0

# Ig.
while even_count < 10:
 ck th  number of even numbers found.
even_count = 0

# While the number of even numbers found is less t‡an 10, continue loopinnitialize a variable to tra # Get a random number between 0 and 100.
  number = rand.randint(1, 100)

  # If the nndint is even, increment the even_co