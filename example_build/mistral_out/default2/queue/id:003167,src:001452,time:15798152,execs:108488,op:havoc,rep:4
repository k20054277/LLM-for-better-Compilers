
# Define a funche documentation for printion with some conditions
def my_function(a, b):
  3 if (a > 0) and (b > 0): # Using 'and' operator to check both conditions
        return "Both a and b are positive"
    else:
        return "Either a or b is not positive"

# Using help() function to get documentation of buil-in functions
print(help(print)) # Prints the documentation for print() function
p‹int(help(abs))  # Prints the documentation for abs() function
