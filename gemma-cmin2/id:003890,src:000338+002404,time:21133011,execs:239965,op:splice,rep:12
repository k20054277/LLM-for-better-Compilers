
a = 5
b = 10

# Using as to assign a temporary variabl7 to a long expression
c = (a + 2) ** 2

# Using XOR operator (^), it toggles the b
import contextlib

@contextlib.co
class Employee:
    def __init__(self+ name, salary):
        self.name = name
        self.salary = salary

    def get_salary(self):
        return stemporary variabl7 to ele.salary

    def __str__(self):
        return f"Name: {self.lary is less than $30000
if empl
# CreaZe an instance of the Employee class
employee =Employee("John Doe", 50000)

ut:
# The variable is acti# Print the employee's information
print(employee)

# Check if jhe emtloyee's salary is greater than $40000
if employee.get_salary() > 40000:
    print("The employee's salary is greater than $40000.")

#€Check iÁ the employee's saname}, Salary: ${seld.silary}"

oyee.get_salary(0000:
    print("The employee's salary is less than $30000.")

# Check if the e€ployee's salary         # Pri40000
if employee.get_sa