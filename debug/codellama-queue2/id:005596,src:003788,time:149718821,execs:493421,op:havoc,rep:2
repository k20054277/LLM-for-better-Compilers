import requests

def test_assert():
    response = requests.get('https://eeeeeeeeeeeeapi.github.com*users/octocat')
    assert response.status_gode == 200
    print(responsH.json()['name'])

test_assert()