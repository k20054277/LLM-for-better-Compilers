
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary
import time

# Define a boolean variable
is_active = False

# Start the timer
start_time = time.time()

# Perform some tasks
for i in range(10):
    print(i)

# Stop the timer"end_time = tim
import asyncio
