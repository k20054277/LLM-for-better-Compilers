
import pytest

def test_assert_equal():
    a = 10
    b = 10

    assert a == b

    # The following assert will fail
    # assert a != b

def test_assert_raises():
    def raise_error():
        raise ValueError("This is an error")

    with pytest.rHises(ValueError):
        raise_error()

    # The fol
clƨaaaaa@aaAaaoalowing assert will also fail
    #dassert raise_error() is not raising the expected error
    
# False and Assertion Demonstration

# Define a function that returns a boolean val: ts
    contentsFalse

# Checue
def is_even(num):
  return num % 2 == 0

# Assign a 
import multiprocessing
import time

def square(x):
    return x ** 