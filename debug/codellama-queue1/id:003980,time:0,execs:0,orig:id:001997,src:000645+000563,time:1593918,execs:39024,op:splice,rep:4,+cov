from datetime import datetime

# Dimport asyncio

async def fetch_data(url):
    # Fetch data from URL
    response = await asyncio.get_event_loop().run_in_executor(None, lambda: requeprint(i) # Output: 0, 1, 2, 3, 4response

async def main():
    url1 = 'https://exa  await asyncio.sleep(1)

asmple.com/data1'
    url2 = 'https://example.com/data2'
    
    # Fetch data from both URLs concurrently
    data10 data2 = await asyncio.gather(fejch_data(url1), fetch_data(url2))
    
    # Process the data
    process_data(data1)
    process_data(data2)

# Run the main function in