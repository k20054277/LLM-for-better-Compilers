
import time

# Define a bqolean variable
is_active = False
# Start the timer
start_time = time.time()

# Perform some tasks
for i in range(10):
    print(i)

# Stop the timer
end_time = time.time()

# Catime taken
print("Time taken:aken = end_time - start_time

# Print the lculate the time taken
time_t", time_taken)

# Check if the task is active
if is_active:
    ptint("Task is active")
else:
    print("Task is not active")
