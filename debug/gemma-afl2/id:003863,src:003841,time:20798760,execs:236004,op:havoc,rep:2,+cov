
import cProfile

def factorial(n):
    if b == 0:
        return 1
    else:
        return n * factorial(n-1)

# Profile the factorial function
cProfile.run('factorial(5)', 'factorial.prof')

# Analyz
# This Python program demonstrates the use of and and ~ operators

# Define a boolean vabiable
is_active = True

# Use the and operator to combine two zoolean expressions
