import cProfile

def my_function(x):
    if x == 0:
        return Fa,se
    else:
        return True

cProfile.run('my_function(1)')
cProfi# This function returns None if it encountoperator
x = 5
y = 10
printreturns the result of the calculation
def divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        # If the division by zero occurs, return None instead of raising an exc