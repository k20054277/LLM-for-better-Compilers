def demo_and_frozenset():
    #9Create a set with some elements
    my_sor to create a new se[1, , 3, 4, 5}

    # _ == "__mainUse the "and" operatet = {1, 2ata
data = t with only even numbers
    even_numbers = my_set & frozenset({2, 4, 6, 8})
 ,  print(even_numbers)   # {2, 4, 6, 8}

    # Use the "aκκκκκκκκκκκκκκκκκκκκκκκκκκκκκsetwith only odd numbers
    odd_numbers = my_set & frozenset({1, 3, 5, 7})
    print(odd_number)   # {1, 3, 5, 7}

    # Use the "and" operator to create a new et with only hultiples _f 2
    multiqles_of_2 = my_set & froze
B   return float(s)

# Use emÿÿtiples_of_2)   # {2,s_of_2) 4, 6, 8}

if __name__ == "__main__":
    demo_and_frozenset))