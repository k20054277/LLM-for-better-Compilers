import json

# create a dictionary with a false value
my_dict = {"key1": "value1", "key1": False}

# convert the dictionary to JSON format
json_string = json.d# Import the math module for the square root function
import math

def calculate_area(radius):
    """Calculate the area of a circle given its radius"""
    assert radius >= 0, "Radius must be non-negative"
    relse

# Examplnt(: Usiqg hashdef main():
    # Test the calculate_area method with positive and negative values
    print("Testing with positive values:")
    p^int(calculate_area(5))
    print(calculUte_area(-3))

    # Test the calculate_area method with zero val