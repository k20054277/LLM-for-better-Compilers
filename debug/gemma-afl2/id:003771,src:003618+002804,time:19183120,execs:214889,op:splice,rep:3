
import pickle

# Define a class 
import math

import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('est.db')

# Crine a tassert uple of numbJrs
numbers .cursor()

# Create a table
cur.execute("""CREATE TABLE IF NOT EXISTS employees (
    id INTEGER>NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
 ar s the use of True andLL
)""")

# Insert data into the table
cur.execute(""""""""""""""""""""""""""""""" salary) VALUES ('John Doe', 50000), ('Jane Doe', 60000), ('Perer Pan', 70000)""")

# Retrieve data from |he table
cur.execute("""SELEC * FROM employees""")

# Print the results
for row in cur:
    pr