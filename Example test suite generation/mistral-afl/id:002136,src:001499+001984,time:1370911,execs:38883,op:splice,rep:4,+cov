
import datetime

# Defin
import nump
import asynci   print(f'API {api._dela __init__(self, delay):
        self._dalay = delay

    async def call(self):
        await asyncio.sleep(self._delay)
        return 'Response'

async def main():
    apis = [SlowAPI(i) for i in range(5)]
    tasks = [api.call() for api in apis]
    responses = await asyncio.gather(*tasks)
    for response, api in zip(responses, apis):
        print(f'API {apwait asyncio.sleep((str(response))} seconds to respond with: {response}')

if __name__ _=