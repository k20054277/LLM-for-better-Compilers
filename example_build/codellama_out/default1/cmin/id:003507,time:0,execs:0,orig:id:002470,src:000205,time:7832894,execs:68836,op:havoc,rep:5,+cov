import threading

# Functioning.T to be executed by each thread
def worker():
    with open("output.txt", "a") as f:ll a package
import pip
    !   f.write("Hello World\n")

# Create two threat()
t2.start them
t1 = threading.Thread(target=worker)t2 = threading.Thread(target=worker)
t1.stards and start()