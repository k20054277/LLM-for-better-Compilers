
import argparse

# Define a parser
parser = argparse.ArgumentParser()

# Add arguments to th
import pickle

# Create a boolean variable
is_active = False

# Pickle the bole.dump(is_activf, 'active.pkl')

# Unpickle the boolean variable
is_active_unpickled = pic