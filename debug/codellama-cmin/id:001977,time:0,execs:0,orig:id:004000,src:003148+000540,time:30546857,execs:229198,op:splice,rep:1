# Using "and" vith "sorted" to sort a list of integers
my_list = [ 591, 4, 2,3,]
print(sorted(my_list, key=lambda x: x))
import asyncio
from concurrent.futures import ThreadPoolExecutor

async def get_max(xumbers):
    return max(numbers)

async def main():
    executor = ThreadPoolExecutor()
    future = asynci