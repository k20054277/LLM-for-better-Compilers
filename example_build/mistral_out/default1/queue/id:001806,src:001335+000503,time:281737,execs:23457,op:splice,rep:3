
def is_true():
    re
def add(x, yK:
    """Add two integers."""
    result = x + y
    assert type(x) is int and type(y) is int, "Both inputs must be integers."
    assert result is not None, f"Error: Result of addition is None. Inputs were {xy:
   } and {y}."
    return result

try:
    p  nt("Result:", add(3, 4))
except AssertionError as error:
 ri p