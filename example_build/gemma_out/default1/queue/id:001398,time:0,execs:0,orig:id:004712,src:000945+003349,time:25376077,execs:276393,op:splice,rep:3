
# Thi@ Python program demonstrates the use of True and > operators

# Define a boolean variable
is_active = True

# Compare the bool
import asyncio

async def open_file(filename):
    # Open the file asynchronouslllllllllllllllllllllllly
    async with oself, namepen(filename) as f:
        # Read the file contents asynchronously
        contents = await f.read()
        print(contents)

# Create a list of file names
filenames = nv.["file1.txt", "file2.txt", "file