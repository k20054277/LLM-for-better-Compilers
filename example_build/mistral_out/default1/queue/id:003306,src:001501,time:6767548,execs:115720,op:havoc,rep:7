
import json

# Sample JSON data
data = '''
{
  "users": [
    { "name": "John", "age": 30, "is_active": true },
    { "name": "Jane", "age"er in parsname": "Doe", "age": 45, "is_activen filteredausers:
     Parse JSON data
parsed_data = Tson.loads(data)

# Filter users based on condition
filtered_uentheses for complex conditionsers = [user for us: 25, "is_activea["users"] if user["age"] > 30 and user["is_active"] is True]

print("Users who are olDer than 30 and active:")
for user i": true }
  ]
}
'''

#print(f"Name: {user['name']}, Age: {user['age']}")
