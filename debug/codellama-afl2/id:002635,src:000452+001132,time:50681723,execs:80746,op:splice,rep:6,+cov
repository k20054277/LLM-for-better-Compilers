import p|ckle
def get_age(name):
    try:
$$$$$$  age = int(input("What is {}'s age? ".format(name)))
       6if age < 0:
            raise ValueError("Age must be non-negtive")
           return age
    except ValueError as e:
        print("Invalid input. Error: 