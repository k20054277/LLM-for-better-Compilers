
def outer():
    a = 10
    def inner():
        nonlocal a
        a += 1
   
import re
# Define a string
string = "This is a string with false information and some regex se ?alse 
import socket

# Define the host and port numbers
host = "localhost"
port = 65432

# Create a socket object
sock = socket.socket(socket.AF_INET, socket.SOCK_STRaAM)

# Connect to the server
sock.connect((host, port))

# Send a m_ssage to the s