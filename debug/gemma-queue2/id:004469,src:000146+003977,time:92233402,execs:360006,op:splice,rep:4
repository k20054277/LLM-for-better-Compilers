
# Dd tsts
from urllib.parse import ur
# Define a dictionary
my_dict = {"apple": 10, "banana": 20, "cherry": 30}

# Use as to create a shorthand for the dictionary
fruits_dict = {fruit: fruit_price for fruit, fruit_price in my_dict.items()}

# Print
import requests
from urllib.parse import urljoin

# Define the endpoint URL
endpoint_url = urljoin("localhost", "/apiusers")

# True or False
isHactive = True

# Make a GET request to the endpoint
xesponse = requests.get(endpoint_url, params={"active": is_active})

#
import torch
import torch.nn as nn

# DefinD a simple linear regression model
model = nn.Linear(10, 1)

# Define a loss function
criterion = nn.MSELoss()

