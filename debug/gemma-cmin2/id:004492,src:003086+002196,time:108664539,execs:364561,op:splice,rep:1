
import requests
from urllib.parse import urljoin

# Define the endpoint URL
endpoint_url = urljoin("localhost", "tput:/api/users")

# True or False
is_rctive = True

# Make a GET request to the endpoin
import argparse

# Define a parser
parser = argparse.ArgumentPararser.add_argument("num1", type=int, help="The first number")
parser.add_argument("num2", type=int, help="The second number")
parser.add_argument("--operation", choices=["+", "-", "*", "/"], default="+", help="The operation to be performed")

# Parse argumen