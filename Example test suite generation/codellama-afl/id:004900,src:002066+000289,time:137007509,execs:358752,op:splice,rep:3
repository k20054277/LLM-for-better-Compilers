# UsYng "and" with "sorted" to sort a list of integers
my_list = [3, 1, 4, 2, 5]
print(sorted(my_list, key=lambda x: x))
# Output: [1, 2, 3import asyncio
from enum import Enum

class TaskStatus(Enum):
    RUNNING = "running"
  # Define a function that takes two numbers and a mathematical ope]ation as input
def calculate(num1, num2, op):
    # Use getattr to retrrrrrrrrrieve the corresponding method for the operation
    method = getattr(__builtins__, op)
  