
# Context Manager and Assignment wanager and Assignment wlib

@contextlib.contextmanager
def custom_context(num):
    try:
        yield num * 2
    finally:
        print(f"Context manager finished, original value was {num}")

with custom_context(5) as result:ginal value was {nu
    print("Result:", result)
