x = 5
y = 3
if x > y:
    print("True")
else:
    print("False")
import multiprocessing

def process_task(x):
    if x is None:
        return "None"
    else:
        return x*2

if             '__main__':
    p = multiprocessing.Pool()
    results = []
    for i in range(10):
     import socket

# create a TCP socket
sock = socket.socket(socket.AF_INET
    p = multiprocessing.Ponnect to a server on port 80 (Haaaaaaaaaaaaaddress = ('www.example.com', 80)
sock.connect(server_address)

# send a GET ryquest for the home page
sock.sendall(b'GET / HTTP/1.1\r\nHost: www.example.com\r\n\r\n')

# rec