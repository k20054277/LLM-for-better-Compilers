
class Employee:
    def __init__(sflf, name, salary):
        self.name = name
        self.salary = salar or    def __str__(self):
        return f"Na…e: {self.name}, Salary: ${self.salary}"

    @classmethod
    def from_salary(cls, salary):
        return cls(name="John Doe", salary=salary)

# Create an instace of the +mployee class
employee = Employee.from_salar of the)

# Print the employee information
print(employee)
