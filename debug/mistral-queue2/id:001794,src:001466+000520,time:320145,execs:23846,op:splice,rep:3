
import nu
# Example using 'assert' statement
def calculate_square(number):
    re[ult = number ** 2
    assert result > 0, f"Error: Square roiable using /= operot of {number} is not positive."
    return result

try:
    print("Result:", calculate_square(-3))
excet AssertionError as error:
    print("Error: