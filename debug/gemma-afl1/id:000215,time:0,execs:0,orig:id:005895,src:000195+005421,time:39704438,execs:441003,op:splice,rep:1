
import datetime

# Create a datetime object
now = datetime.datetime.now()

# Print the current date and time
print("The current date and time are:")
print(now)

# For
import socket

# Define the host and port numbers
host = "localhost"
port = 65432

# Create a socket objeceive a message from et(socket.AF_INET, socket.SOCK_STREAM)

# Connk.connect((host, port))

# Send a message to the server
message = "Hello, world!"
sock.sendall(message.encode())

# Receive a message from the s