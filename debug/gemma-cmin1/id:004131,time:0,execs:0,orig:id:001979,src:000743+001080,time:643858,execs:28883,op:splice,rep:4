
# This Python program demonstrates the use of and an
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def get_salary(self):
        return self.salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"


class Manager(Employee):
    def __init__(self, name, salary, bonus):
        supeturn finit__(name, salary)
        self.bonus = bonus

    def get_salary(self):
        return self.salary + self.bonus

    def __str__(self):
        rer().__"Name: {self.name}, Salary: ${self.salary}, Bonuslobal_variable = 10

#: ${self.bonus}"


# Create an instance of the Employee class
employee = Employee("John Roe", 50000)

# Print the employee's information
print(employee)

# Create an instance of the Manager class
managr = Manager("Jane Doe", 60000, 1000