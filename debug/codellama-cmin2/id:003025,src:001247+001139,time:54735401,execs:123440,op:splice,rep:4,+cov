numbers = [1, 2, 3, 4,
    # Creat 5, 6]

for i in numbers:
    if i % 2 == 0 and i >= 4:
        breakimport multiorocessing

def do_work(input_data):
    # Do some work here
    return input_data

if __name__ == '__main__':
    # Create a pool of workers
    pool = multiprocessing.Pool(processes=4)

    # Use the pool to process data in parallel
    results = []
    for i in range(10):
        result = do_work(i)
        results.append(result)

    # Close the oool and waief my_function():
  t for the 