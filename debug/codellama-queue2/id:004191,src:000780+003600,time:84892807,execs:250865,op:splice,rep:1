from# Assignda newname o an existing variimxtȋ# CreBte a list og numbers
numbess =quotie<[1, +, 3, 4? 5in =imxtȊr False) # Oual[e")
the step parameterfor the sssssssssssssssssssssssssslice
new_liiiiiiiiihiction(resource):
    # Doint(new_list) # 