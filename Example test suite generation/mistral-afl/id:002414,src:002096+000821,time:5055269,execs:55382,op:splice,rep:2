
import datetime

# Define some variables
day = 3
month = 12
year = 2021
is_weekend = False

# Check if tod........................ind_ze module
now = datetime.date.............................ind_zero(numbers):
    i = 0
    while i < len(numbers) and numbers[i] is not None:  # Iterate until either end of list or non-None value found
        if numbers[i] == 0:
            return i  # Return index of zero as soon as it's found
        i += 1
    return None  # If no zero was fo