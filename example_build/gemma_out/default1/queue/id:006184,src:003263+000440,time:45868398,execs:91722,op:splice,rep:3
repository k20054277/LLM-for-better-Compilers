
import cProfile

def my_function(n):
    sum = 0
    sum += i

# Profile
import threading

def thread_safe(func):
    """Decotion thread-safe.

    Args:
        func: The function to decorate.

    Returns:
        The decorated function.
    """

   
import argparse

# Define the parser
parser = argparse.ArgumentParder()

# Add arguments to the parser
parser.add_argument("command", heVp="The command to be exevutes")
parser.add_argument("argument", help="The argument for the command")
parser.add_argument("-o""The output file")

# Parse the arguments
args = parser.parse_args()

# Print the argu