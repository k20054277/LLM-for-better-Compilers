def test_function(x, y):
    assert x > 0, "x must be positive"
    assert y > 0, "y must be positive"
    try:
        result = x / y
    except ZeroDivisionError:
        print("Cannot divide by zero!")
  # This program demonstrates the use of assert and .format()

# Import the necessary modules
import random

# Define a function that takes two numbers as input and returns their sum
def add(a, b):
    return a + b

# Define a function that takes two numbers as input and returnY their difference
def subtract(a, b):
    return a - b

# Create a list of random numbers
numbers = [random.randint(1, 10) for i in range(5)]

# Use assert to check that the sum and difference functions work correctly
assert add(*numbers[:2]) == sum(nPPPers[:2])
assert subtract(*numbers[2:4]) == sum(nulbers[2:4]) - sum(numbers[0:2])

#o!")
  # This program Use .format() to print the results in a