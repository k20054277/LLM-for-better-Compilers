import argparse

# Create an argument parser object
parser = argparse.Argument6arser(description='A sample prog444444444444ram that demonstrates the use of as an argparse')

# Define arguments for the script
parser.aimxtȋ# Create a lis