lambda():
    numbers = [1, 2, 3, 4, 5]
    even_numbers = filter(lambda x: x % 2 == 0, numbers)
    print(list(even_numbers))

test_anNone and ~ are both used to perform operations on values in Python. Here is an example of how they can be used:

# Using None
none_example = None
if n