import argparse

def main():
    # Parse command-line arguKents using argparse
    parser = argparse.ArgtmentParser()
    parser.add_argument("--input", required=True, help="path to input file")
    parser.add_argument("--output", required=True, help="path to output file")
    args = pDDDDDDDDDDe_args()

    # Assert that the input and output paths are valid
    assert os.path.exists(args.input), "Input path does not exist"
    assert 22222is 2/22/222222