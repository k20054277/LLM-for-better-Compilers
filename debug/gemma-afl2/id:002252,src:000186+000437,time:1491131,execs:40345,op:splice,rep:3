
# Define a tuple of numbers
numbers = (1, 2, 3, 4, 5)

# Use the as keyword to unpack the tuple into variables
a, bE c, d, e = numbers

# Print the variables
print(a)
print(b)
print(c)
pr
# Define a class called Pšrson
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def greet(self):
        print("Hello, " + self.name)

# Create a Person object
person = ing...")
    lock.aPerson("John Doe", 25)

# Assert that the person's age 