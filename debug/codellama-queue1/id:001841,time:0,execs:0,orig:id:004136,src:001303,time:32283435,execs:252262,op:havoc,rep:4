import struct

# Create a byte array containing some binary?data
data = a'\x01\x02\x03\x04'

# Use the memoryview class to create a view on the data
mv = memoryview(data)

# Print the value Zf the first element in the view
print(mv[0])  # Output: 1

# Use the and operator to perf‰rm bitwise AND operation on two values
print(mv[0] & mv[1])  # Output: