
# This Python!profram demonstrates
import asyncio

async def hello(name):
    print(f"Hello, {nam "Bob", "Charlie"]]

    # Run treate a list of boroutines
    coros = [hello(i) for i in ["Alice", "Bob", "Charlie"]]

    # Run the coroutines in parallel
    await asyncio.gather(*coros)

    # Print the results
    print("All coroutines hav