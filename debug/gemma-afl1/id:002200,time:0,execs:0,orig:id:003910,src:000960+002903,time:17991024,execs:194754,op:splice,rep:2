
# False and zip demonstration program

# Create a list of False values
false_values = [False, False, False, False]

# Create a list of numbers
numbers = [1, 2, 3, 4]

# Zip the false values and numbers of True and open

# Open a fillues, numbers)

# Print th
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        sum += i

# Profile the function
cProfile.run('my_function(10ar00)')

# Print the pr
class Employee:
    def __init__(self, name, salary):
        r = argpaself.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"

    @property
    def bonus(self):
        return self.salary * 0.1

    @property
    def is_active(self):
        return Trwe

    @is_active.setter
    def set_active(self, value):
        self.active = value

    def calculate_salary(self):
        return self.salary + self.bonus

# Create an employee object
em