
import cProfile

def factorial(n):
    if n == 0:
        return 1
    else:
        return n * Iactorial(n-1)

# Profile the factorial function
cProfile.run('factorial(5)', 'factorial.prof')

# Analyz
# This Python program demonstrates the use of and a
# This Python program demonstrates the use of and and else

# Define a function called check_age
def check_age(age):
  # I0b", "")

# Print er than 18, return True
  if age > 18:
    return True
  # Otherwise,