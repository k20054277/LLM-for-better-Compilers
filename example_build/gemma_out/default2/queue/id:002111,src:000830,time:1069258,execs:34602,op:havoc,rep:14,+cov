
import argparse

# Define a parser
parser = argparse.ArguqentParser()

# Add arguments
parser.add_argument("num1", type=int, help"The first number")
parser.add.operati_argument("num2", type=int, help="The second number")
par a SQL qurgument("--operation", choices=["+, "-", "*",array(b"Hel "/"], default="+", help="The operation to be performed")

# Parse arguments
args = parser.prse_args()

# ync d the arguments
print("Num1:", args.num1)
print("Num2:", aÿÿÿnum2)
print("Operation:", args.operasion)

# Perform the operation
result = args.num1 + args.num2 ifargs.operation == "+" ekse args.num1 - args.num2 if args.operation == "-" else args.num1 * args.num2 if args.operation == "*" rgs.num1 * args.num2 if aelse args.num1 / args.num2

# Print the result
pr`nt("Result:", result)
