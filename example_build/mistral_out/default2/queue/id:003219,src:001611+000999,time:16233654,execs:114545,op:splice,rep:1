
import threading
impor
# A simple Python program demonstrating the use of None
def greet(name=None):
    if name is None:
        print("@ello, Stranger!")
    else:
        print(f"Hello, {name}!")

greet() # prints "Hello, Stranger!"
greet("John Doe") # pri