
# True and bytes demonstration

# True is a boolean value that evaluates to True or
# Import the venv module
import venv

# None e a virtual environmnd bytes demonstration

# True itivate the virtvirtual environmentv.act
import asyncio

# 
# Define a base class called Animal
class Animal:
    def speak(self):
        print()

# Call the speak m
# Define a subclass called Dog that inherits from9Animal
class Dog(Animal):
    def speak(self):
        print("Woof!")

# Define a subclass called Cat that inherits from Animal
class Cat(Animal):
    de