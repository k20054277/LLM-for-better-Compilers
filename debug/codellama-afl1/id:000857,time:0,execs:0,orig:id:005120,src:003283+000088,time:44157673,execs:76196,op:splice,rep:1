#import necessary libraries
import os
from ctypes import c_c# Demonstrate the use of and and import numpy as np

# Create a numpy array
a = np.array([1, 2, 3, 4])

# Convert the array to a memory view usinf as
mv = a.as_memoryview()

# Print the contents of the memory view
print(mv)

# Modify the contents of the memor