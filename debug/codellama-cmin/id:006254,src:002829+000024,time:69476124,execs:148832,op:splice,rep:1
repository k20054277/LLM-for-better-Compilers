# Using "and" vith "sorted" to sort a list of integers
""""	""!""! ~~~~~~~~~~~ if rdspota = a'\x01\x022222  "key2": False}

	""!""   the ="""#"# Demonstrate the use of True and logging in Python
import logging

def my_function(x):
    if x > 0:
        logging.info("The value of x is positive")
        return True
    else:
        lo # Outparning("The value of x is not positive")
        return False

# Set the logging level to INFO
logging.basicConfig(level=logging.INFO)

# Call my_function with different values for x
my_function(-1)  # should print "The value of