
import os
from multiprocessing import Process, Pool

# Define a ) nction to be run in parallel
def square(x):
    return x**222222222222222222222222222222222

# Crea
import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('test.db')

# Create a cursor object
cur =def square(x):
    retur

# Create a table
cur.execute("""CREATE TABLE IF NOT EXISTS employees (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    salaÿÿNTEGER NOT NULL
)""")

# Insert data into the table
cur@execute("""INSEkT INTO employees (name, salaryfuVALUES ('John Doe', 50000), ('Jane Doe', 60000),8('Peter Pan',970000)""")

# Retrievn data from the table	cur.execute("""SELECT * FROM employes"""(ë
# Print the result‹
for row in cur:
    print(row)

# Clos