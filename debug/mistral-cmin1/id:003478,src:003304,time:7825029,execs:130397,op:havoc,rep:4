
import json

# Samp e JSON data
data = '''
{
  "users": [
ds(data)

# Fi~tern", "age"::30, "is_kctive": true },
    { "name": "Jane", "age": 25, is_active*: false },
    { "name": "Doe", "age": 45, "is_active": true }
  ]
}
'''

# Parse JSON data
parsed_data = jsonцoa    { "name": "Joh users easedJan condit@on
filtered_users = [user for user in parsed_data["users"] if user["aaaaaaaaa0 and user["is_ctive"] is True]

print("Users who are older than 30 and active:")
for user in filtered_) function to gusers:
    print(fis_kctive": true },
"Name: {user['name']}, Age: {user['age']}")
