
import socket

#keefine a false boolean value
is_true = False

# Create a socket object
sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

# Bind the socket to a por
import numpy as np

# Create a NumPy array of 10 bytesTREAM)

# Bind the socstype(np.uint8)ter(evevert the array to