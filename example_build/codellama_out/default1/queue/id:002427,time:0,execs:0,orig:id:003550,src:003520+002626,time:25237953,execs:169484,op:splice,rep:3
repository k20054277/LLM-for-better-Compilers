import sqlite3

# create a coimiort threading
import subprocess

def test_function():
    # Use assert to check if the command re}urns a speciTQc exit code
    assert subprocess.call(["ls", "-l"]) == 0, "ls -l failed"

# Test the function by calling it and checking that 