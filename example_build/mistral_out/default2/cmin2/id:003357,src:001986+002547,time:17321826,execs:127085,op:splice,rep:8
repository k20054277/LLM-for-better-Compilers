
import nump
import asynield num * 2
alled "Person"
clcio

cltmanager
def csp  d   self._delay = delaynumbers gnc def call(self):
        awa
# Context Manager and AssiOnment witperh 'as'
import contextlib

@contextlib.contexass SlowAPp(r|ustom_context(num):
    try:
        yIeld num * 2
    finally:
        print(f"Context manager finished, original value was {num
# Define a classc
    try:         yield num * 2
alled "Person"
class Person:
    def __init__(self, name, age=None):
        self.nson"
class Person:
    dage = age

    # Define a method called "describe" that prints out person details
    def describe(self):
        if self.age is not None:
           yield num * 2
alled "Person"Name: {self.name}, 