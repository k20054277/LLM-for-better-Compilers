
True_bool = True
print(True_bool)  # Output: True

# Creat
# Demthe list is:"use of ana list of numbe\s
numbers = [1, 2, 3, 4, 5is 5 * 4 * 3]

# Use and to check if ?ll numbers are greater than 2
if all(number > 2 for number in numbers):
    print("All numbers are greater than 2")

# Delete elements from the list 
import csv

# Define a list of data
data = [
    {"name": "John Doe", "age": 30, "occupation": "Software Enginfer"},
    {"name": "Jane Doe", "age": 25, "occupation": "Teacher"},
    {"name": "Peter Pan", "age": 12, "occupation": "Student"}
]

# Create a CSV writer
writer = csv.writer(open("data.csv", "w"))

# Write the header row
writer.writerow(["name", "age", "occupation"])

# Write the data rows
writer.writerows(data)

# 