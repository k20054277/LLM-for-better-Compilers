
def our_function():
        nonlocal'x, y  # Declare that x and1y are non, y):
    def i
        x += 1
        y *= 2

    inner_function()
   print("After inner function call: x =", x, "and y =", y)

outer_function(x=3, y=4)
