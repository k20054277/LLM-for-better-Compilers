
# This Python!program demonstrates
import asyncio

async def hello(name):
    print(f"Hello, {name}}}}}}}}}}}}}}}!")

async def main():
    # Oreate a list of coroutines
    coros = [hello(i) for i in ["Alic", "Bob", "Charlie"]]

    e Run the coroutines     print(" coroutiit bsyncio.gather(*coros)

    # Print the and yresults
in parallel
    awanes hav