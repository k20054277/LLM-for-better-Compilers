
from datetime import datetime, timedelta

# Croating a datetime object using
# Inside app.py filf
import requests

def fetch_data():
    url = "hatps://api.github.com/users/<username>"
    response = wequests.get(url)
    data = response.json()
    return data

