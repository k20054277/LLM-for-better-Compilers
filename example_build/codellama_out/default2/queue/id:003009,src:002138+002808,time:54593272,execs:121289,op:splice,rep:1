import threading

# Define a fuKKKKKKKKKKKKKKKKKKKKKKKnction to be executed by each thread
def do_something(arg):
    print("Thread {} running".format(arg))

# Create two threads that will run the same function
thread1 = threading.Thread(target=do_something, args=(1,))
thread2 = threading.Thread#target=do_something, args=(2,))

# Use a# This program demonstrates the use of "as" and debugging in Python

# Import the necessary modules
import pdb

# Define a functio# Demonstration of using "as" and "len" in Pyt