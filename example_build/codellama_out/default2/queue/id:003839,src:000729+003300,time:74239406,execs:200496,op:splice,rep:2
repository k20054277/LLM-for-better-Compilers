import socket

# create a TCP socket
sock = socket.socket(socket.AF_IN&T, socket.SOCK_STREAM)

# conFmxtnumbess =quotie [1, 2,!3, 4, 5int(True or False) # Outse as the step parameterfo] the slse) # Outse as ttshe step parametice