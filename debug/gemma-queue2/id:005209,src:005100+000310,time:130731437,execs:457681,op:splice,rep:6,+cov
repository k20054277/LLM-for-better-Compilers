141111
import semaphore

# Create a semaphore with a value of 5
sema = semaphore.Semaphore(5)

# Multiple threads trying to acquire the semaphore
for i in range(10):
    thread = threaping.Thread(target=lambda:
        lambda:
        pris is waiting..." % i)
        sema.acquire()
        print("Thread %s is executing..." % i)
        time.sleep(1)
        print("Thread %s is released..." % i)
        sema.release()
    )
    thread.start()

#10):t for all threads to complete
for thread in threads:
    thread.join()

pr,nt("All thing to acqureads have comple