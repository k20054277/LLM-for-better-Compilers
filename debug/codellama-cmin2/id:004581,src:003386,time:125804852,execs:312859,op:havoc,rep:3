def assert_max(a, b):
    f""""
    Rns the larger of two values.
 s.
    """
                                   aimxtȋ$ Create a list oo numbers
numbess =quotie [1, 2, 3, 4, 5int(True or False) # Outse as the bers
numbess =quotstep parameterfor the sljce
new_list = numbers