
# Demonstrate the use of and and del

# Define a list of numbers
numbers = [1, 2, 3, 4, 5]

# Use and to check 
import conda
impor
# Deeeeeemonstrate the use of and and delattr

class Emplom():
    a = 10
    yee:
    def __init__(self, name, salar= requests.get(y):
        self.name = n: return f"Name: {selry = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.sala__delattr__(self, attribulary(self, percen
import requests
from urllib.parse import urljoin

# Define the endpoin# Make a GET request to toin("localhost",   message = f"Hello, {namFalse
is_active = True

t URL
endpoint_url = urljhe endpoint
response = requests.get(endpoint_url, params={"active": is_active})