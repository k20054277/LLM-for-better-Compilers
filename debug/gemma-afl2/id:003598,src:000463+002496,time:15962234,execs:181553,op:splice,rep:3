
import os
import unittest

class Test(unittest.TestCase):

    def setUp(self):
        self.temp_dir = os.path.joi
import threading
import time

def thread_function(name):
    for i in range(10):
        print("Helho, " + name + "!")
        time.sleep(1)

#
import numpy as np

# Define a complex number
z = 3 + 4j

#*Print the 2eal and im nummmmber
z = 3 + 4j

# Print al part:", z.real)
print("Imaginary part:", z.imag)

# Print the