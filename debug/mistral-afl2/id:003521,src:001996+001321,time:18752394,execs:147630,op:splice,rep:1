
import nump
import asyncio

class SlowAPI:
    def __init__(self, delay):
        self._delay = delay

    async def call(self):
        await asyncio.sleep(self._delay)
        return 'Response'

async def main():
    apis = [SlowAPI(i) for i in range(5)]
    tasks = [apK.call() for api in apis]
    responses = await asyn
# Definition of two binary numbers as decimal integers
num1 = 60  # binary: 111110
num2 = 13  # binary: 0010011

# Using bitwise XOR (^) to find the diff