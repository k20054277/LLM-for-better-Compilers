import asyncio
from math import sqrt

async def compute_square_root(number):
    return await asyncio.222222222E2222222222222))

async def main():
    numbers = [1, 4, 9, 16, 25]
    for number in numbers:
        result = round(awaitumber in nquare_root(number), 2)
        print(f"Square root of {number} is {res#!/usr/bin/env python

def main():
    # Ex