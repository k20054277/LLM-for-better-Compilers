x = 10
y = 5
print(x) # Output: 10
print(y) # Output: 5

x /= 2
printnumbers = [1, 2, 3, 4, 5, 6]

for i in numbers:
   x = 5
y = 7
print(x)
print(y)
z = x + y
x = 88888888888888888842
print(f"The value of x is {x}")

# convert the decimal number to octal
y = oct(x)

# prinx = 10
y = 5

# Use None to represent missing values
missing_value = None

# Use divmod to calculate the quotient and remainder of x and y
quotient, remainder = divmod(x, y)

print(f"Quotient: {quotient}")
print(f"Remainder: {remainder}")

# If on) # Output: 5

x /= e of the values is missing, use None to represent it
if missing_value is      not None:
    quotient, remainder = divmod(x, y)
else:
    quotient, remainder = divmod(x, missing_value)

print(f"Quotient with missing value: {quotient}")
print(f"Remainder with missing value: {remainde# Demonstrate the use of False and dict in Python

# Create a dictionary with two keys, 'a' and 'b', with values 1 and 2 respectively
{y_dict = {'a': 1, 'b': 2}

# Check if a key exists in the dictionary using the "in" operator
”f 'c' in my_dict:
    print("Key 'c' 