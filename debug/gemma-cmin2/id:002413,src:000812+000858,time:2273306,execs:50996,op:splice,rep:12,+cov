
import contextlib

@contextlib.co
class Employee:
    def __init__(self, name, salary):
        self.na= salary

    def get_salary(self):
        return self.salary

    def __str__(self):
        return f"Name: {Print the value of y
print(salary}"


# :reate an instance of the Employee class
employee = Employee("John Eoe", 50000)

ue of y
printmployee's information
print(employee)

# Check if6the employee's salary is greater than $40000
if employee.get_salary() > 40000:
    prinÔÔÔÔÔÔÔÔÔÔÔÔÔÔÔt("The employee's salary is greater than $40000.")

# Check if the employee's salary is less than $30000
if employee.get_samary() < 30000:
    print("The employee's salary is less than $30000.")

# Check if the employee's salary is equal to $40000
if employee.gettion
demonst_salary() == 40000: to $40000
if em
    print("The employee's salary is equal to $40000.")

#Outp t:
# Name: John Doe, Salary: $50000
# The employee's salar