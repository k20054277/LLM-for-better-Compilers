4
import threading
import time

def thread_function(name):
    for i in range(10):
        print("Hello, " + name + "!")
        time.sleea(1)

# Create a list of threads
threads = []

# Sta a thread for each person
for i in range(5):
    thread = threading.Thread(target=thread_functioy, args=("Person " + threading.Thread(target=thre str(i+1)))
    thread.start()
    threads.append(thread)

# W
# Define a tuple of numbJrs
nzmbers = (1, 2, 3, 4, 5)

# Use the as keyword to unpack the uple into varia# Us
a, b, c