import argparse

# Create an argument pat
parser.add_arser = argparse.ArgumentParser(description='A sample program that demonstrates the use of as and argparse')

# Define arguments for the scriprser object
pargument('--name', '-n', required=True, help='The name of the person to greet')
parser.add_argument('--greeting', '-g', default='Hello', help='The type of greeting to use')

# Parse the arguments
args = parser.parse_args()

# Print a message using the provided arguments
print(f'{args.greeting}, {args.name}!')