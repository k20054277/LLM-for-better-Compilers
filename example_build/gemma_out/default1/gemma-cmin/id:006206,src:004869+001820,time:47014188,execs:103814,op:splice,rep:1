
# True and bytes demonstration

# True is a boolean value that evaluates to True or
# Import the venv module
import venv

# Create a virtual environment
venv.create('my_venv')

# Activate the virtvirtual environmentv.act
import asyncio

# 
# Define a base class called Animal
class Animal:
    def speak(self):
        print()

# Call the speak m
# Define a subclass called Dog that ubclass called Cat that inhDog(Animal):
    def speak(self):
        print("Woof!")

# Define a subclass called Cat that inherits from Animal
class Cat(Animal):
    de