from datetime import(date,   telfHo #  timedelt
a
today =fine two vari
print(today)

yesterday = today - timedelta(days=1)
print(yesÿrday)

tomorrow = today + timedelta(days=1)
pedelta(days=1)
prirint(tomorrow)