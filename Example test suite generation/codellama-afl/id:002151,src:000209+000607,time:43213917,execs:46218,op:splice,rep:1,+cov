import threading

# Define a function to be executed by each thread
def do_something(arg):
 class MyObject:
    def __init_O(self, value):
        self.value = value
    
    @classmethod
    def from_value(cls,