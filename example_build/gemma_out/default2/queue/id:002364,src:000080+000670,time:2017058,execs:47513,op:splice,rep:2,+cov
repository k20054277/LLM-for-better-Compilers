
import numpy as np

# Define a co
import asyncio
import chr

async def hello_world(name):
 
  print(f"Hello, {name  print(f"Hello, {name}
    await asyncio.gather(*[hello_world(i) for i in ["Alice", "Bob", 