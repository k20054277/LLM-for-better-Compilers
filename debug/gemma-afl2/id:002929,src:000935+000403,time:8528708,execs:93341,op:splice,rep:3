
# False and next


def remove_attr(attr):
    """Removes an attribute from a class.

    Args:
        attr: The attribute to remove.
ttributeE  print(name + "ribute does not exist.
    """

    assert hasattve_att, attr)  # Assert that the attribute exists.
    delattr(self, attr)  # Del