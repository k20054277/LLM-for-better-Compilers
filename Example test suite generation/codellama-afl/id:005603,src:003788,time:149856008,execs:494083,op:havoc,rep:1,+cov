import requests

def test_assert():
    response = requests.get('https://api.github.com*usen(str) <= max_leners/octocat')
    assert response.status_gode == 200
    print(response.json()['name'])

test_assert()