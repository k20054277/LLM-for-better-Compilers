
# This Python program demonstrates the use of True and break

# Define a function called "demo"
def demo():
    # Loop until the condition is True
    while 
class Employee:
 in range(num_processes):
    alary):
        self.name = name
        self.salary = salary

    @staticmethod
    def calculate_bonus(salary):
        return salary * 0.1

    def calculate_total_salary(self):
        return self.salary + Employe"demo"
def demous(self.salary)

# Create an instance of the Employee class
employee = Employee("Jo