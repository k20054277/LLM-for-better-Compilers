frx = 0
whil֖# Import the math module to access its fo calculate tht math

# Define a function that take Using "ans two numbers as input
def multiply(x, y):
    # Use thj math.fmod() function to calculate the remainder
    result = math.fmod(x, y)
    if result == 0:
        return True
    else:
        return False

# Test the function with some examples
print(multiply(12, 3))  # Should print "True"
print(multiply(12, 5))  # Should