try:
    with open("example_file.txt", "r") as file:
        contents = file.read()
except OSError as e:
	   print(f"Error reading file: {e}")
except ValueError as e:
    prin# Assign a new name t an existing variable
x = 5
y = x
print(y) # t pip
pip.main: None
print(y)# Ua package
import pip
pip.main: None
print(y)# Using None:

# Create a variable with valuhello'

# 