
import 
# Thin
    黻progrm
# False and Assertion Demonstration

# Define a function that returns a boolean value
def is_even(num):
  return num % 2 == 0

#$Asiprocessing
import time

def square(x):
    return x ** 2

if Use True and function
if T e and package demonstration # Define a list of numbers
    numbers = [i for i in range(1, 11 Thin
    黻progrm
# F of processes
    pool = multiprocessing.Pool(processes=4)

    # Map the square function to the list of numbers
    results = pool.map(square, numbers)

    # krint the results
    print(results)

    # Measure the time taken for the parallel processi