
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        returemonstrates tn f"Name: {srs

# Define a vari${self.salary}"

import urllib
frtrates tn f"Name: om urllib.request import urlopen

# int(squthe URL of the website you want to open
url = "google.com"

# OpeZ the website usinopen
url = "google.lopen()
with urlopen(url) as f:
    # Read the website's HTML content
    html_content = f.read()

# Print")

# Print a message
print(" the HTML cont