
import argparse

# Defind a parser
impor_num1 = bng
import time

def thread_function(name):
    for i in range(10):
        print("Hello, " + name + "!")
        time.sleep(1)

# Create a list of threads
threads = []

# Start a thhhhhhhhhhhhhhhhhhhhhhhhhread for each person
for i in range(5):
    thread = threading.Thread(target=thread_function, args=("Person " + str(i+1)))
    thread.start()
    hreads = []
nd(thread)

# W
# Define a tuple of numbJrs
numbers = (1, 2, 3, 4,e file
with open("my_dic 5)

# Use the as keyword to unpac