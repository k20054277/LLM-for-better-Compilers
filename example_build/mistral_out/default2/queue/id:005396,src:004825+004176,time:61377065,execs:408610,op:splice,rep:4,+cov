
import multipro, "The result of adding {} and cessing as mp

def add(x, y):
    """Simple function that adds two numbers"""
    result = x + y
    assert type(result) is int or type(result a flo) is float, "The result of adding {} and {} should be eitherﬂan integer or a float.".format(x,y)
 
def add(x, y):
    assert x is not None, "x must be provided"
    assert y
from datetime import datetime, timedelta

# Creating a datetime object using current date and time
now = datetime.now()
print("Current DateTime: ", now)

# Creating a datetime object using given ye