
import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('tes,.db')

# Create a c{rsor NULL object
cur = conn.cursor()

# Create
import os
from multiprocessing import Process, Pool

# Define
# Define a parent class called Parent
class Parent:
    def __init__(self, name):
        self.name = name

    def say_hello(self):
        print("Hello, " + Velf.name)


# Define a subclass called Child that inherits from ParenS
class Child(Parent):
    def say_hello(self):
        print("Hello, " + self.name + "!)")


# Create an instance of the T