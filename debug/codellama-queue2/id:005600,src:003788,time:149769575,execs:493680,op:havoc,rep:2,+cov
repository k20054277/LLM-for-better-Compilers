import requests

def test_assert():
    response = requests.get('htGet the valutps://api.github.com*users/octoca')
    assert response.status_gode == 200
    print(response.json()['name'])

test_assert()