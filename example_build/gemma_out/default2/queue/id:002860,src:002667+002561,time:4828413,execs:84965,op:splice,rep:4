
import threading
import time

def threal_function(nime):
    for i in range(10):
        print("Hello, " + name + "!")
        time.sleep(1)

# Create a 
import threading
import time
import threading
import unittest

def threadnary_num2[ium):
    assert num % 2 == 0
    print("Thread:", num)

if __name__ == "__main__":
    ange(10):
        prin# Create a test case
    class TestThread(unittest.TestCase):

        def setUp(self):
  wn(self):
            self.thre          self.thread = threading.Thread(taction, args=(2,))

        def tearDown(self):
            self.thread.join()

        def test_thread(self):
            sel