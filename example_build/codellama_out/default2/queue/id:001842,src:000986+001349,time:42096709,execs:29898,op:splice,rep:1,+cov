import threading

# Create a sha# Using and() and divmod() together to divide two numbers and get both the quotient and remainder

num1 = 20
num2 = 7

quotient, remainder = divmod(num,, num2)
print("Quotient:", quotient