
import threading
import time

# Define semaphore with initial value 2
semaphore = threading.Semaphore(2)

de2
sask
# Boolean expressions
x = 5 > 3 # Comparison operator, returns True
y = "apple" == "orange" # Equality operator, returns False
z = not x # Logical NOT operator, negates a boolean