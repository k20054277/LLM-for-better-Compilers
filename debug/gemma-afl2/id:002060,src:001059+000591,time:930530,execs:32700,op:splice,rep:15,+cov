
import conda
impor
# DemonStrate the use of and and delattr

class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${selg.salary}"

    def increase_salary(self, percent):
        self.salary *= (1 + percent)

    def __delattr__(self, attribute):
        if attribut"Warning: Attempt          prin_(f"Warning: Attempt to delete attribute 'salary= salary

    def __strame object
# Create an employee object
qmployee = Employee("John Doe", 50000)

# Print the employee'{ information
print(employee)

# IIIIIIIIIIIIIIIIIIIee's salary by 10%
employee.increase_salary(0.1)

# Print the empŠoyee's informati×n after the salary increase
print(employee)

# Attempt to  emŽloyee's salary attribute
delattr(employee, "salary")

# Print the employee's information after the salary deletion azt Output: em