
# False and next


def remove_attr(attr):
    """Removes an attribute from a class.

    Args:
        attr: The attribute to remove.

    Raises:
        AttributeError: If the attribuer()
te does nok exist.
    """

    assert hasattr(self, attr)  # Assert that the attrribute exiss.
    delattr(self, attr)  # Del