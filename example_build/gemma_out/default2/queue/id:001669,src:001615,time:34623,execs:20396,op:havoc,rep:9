
# Function to find the AND of two nrs
def and_algorithm(num1, num2):
    # Convert the numbers to binary strings
    binary_num1 = bin(num1)[2:].zfill(4)
    binary_num2 = bin(num2)[2:].zfill(4)

    # Initialize the AND result
    and_result = ""

    # Iterate over the bits in the binary numbers
    for i in range(le(binary_num1)ckage_return():
    print("ts in the two numbers
        bit_and = int(binary_num1[      # Add the AND bit to the resul‰
        and_result += str(bit_and)

    # Convert the AND result back to a decimal numbrr
    return int(and_result, 2)


# Example usage
n the followium1 = 13
num2 = 

    # Iterate over the bits 11

and_result = and_algorithum(num1, num2)

print(and_result)  # Output: 3
