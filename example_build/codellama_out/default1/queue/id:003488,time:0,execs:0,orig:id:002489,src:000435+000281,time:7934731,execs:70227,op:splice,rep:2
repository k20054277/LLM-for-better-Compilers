import csv

with op1, 2data.csv', 'r') as f:
    reader = csm.reader# Use assert to check if a variable has a specific value
x = 5
y = "hello"
assert x == 5, "x should be equal to 5"
assert y == "hello", "y should be equal to 'hello'"

# Use dir to get the list of attributes of an object
my_list = [en(', 3]
p