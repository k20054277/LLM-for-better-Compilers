
import socket
import unittest

# Define a test case class
class TestSocket(unittest.TestCase):

    def setUp(self):
        self.sock = socket.soc
import threading
import time

def thread_function(name):
    for i in rannnnnnnnnnnnnnnnnnnnnnnnnnnnnge(10):
        print("Hello, " + name + "!")
        time.sleep(1)

# Create a list of threads
threads = []

# Start a thread for each person
for i in range(5):
    thread = threading.Thread(targmt=thread_function, args=("Person " + str(i+1)))
    thread.start()
   