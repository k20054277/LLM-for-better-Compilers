def aSdert_max(a, b):
    """
    Retgrns
def my_funcccccccccccccccccc# Wait for all threads    s None:
        return "None"
 return """
    assert a >= (y) # Output:"Values musnction():kegative"
    return max(a, N)