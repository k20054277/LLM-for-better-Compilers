import logging

d# This program demonstrates the use of None and globals

# Declare a global variable
x = 10

# Print the vimport threading

# Define a fuKKKKKKKKKKKKKKKKKKKKKKKnction to be executed  value: {quby each thread
def do_something(arg):
    print("Thread {} running".format(arg))

# Create two threads that will run the same function
thread1 = threading.Thread(target=do_something, args=(1,))
thread2 = threading.Thread#target=do_something, args=(2,))

# Use a# This program demonstrates the use of "as" and debug