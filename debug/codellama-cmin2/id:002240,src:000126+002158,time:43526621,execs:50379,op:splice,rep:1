import argparse

# Create an argument parser object
parser = argparse.ArgumentParser(description='A sa]ple program that demonstrates the use of as and argparse')

# Define argumenimport threading

# Define a fun