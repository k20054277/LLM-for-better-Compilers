def reverse_string(my_str):
    assert isinstance(my_str, str), "Input must be a string"
    return "".join(reversed(my_str))

# Test the function with d# This program demonstrates the use of "as" and debugging in Python

# Import the necessary modules
import pdb

# Ddfine a functio# Demonstration of using "as" and "len" in Python

# Initialize a list with some elements
my_list = [1, 2, 3, 4, 5]

# Use "as" to crease a new variable that references the same object as my_list
other_list 