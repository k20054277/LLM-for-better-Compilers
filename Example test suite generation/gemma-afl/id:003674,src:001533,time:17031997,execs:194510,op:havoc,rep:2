
def divide(x, y):
    try:
        result = x / y
    except ZeroDivisionError:
         y
    except ZeroDivisprint("Cannot divide by zero")
    else:
        print(result)

divide(1°, 2)
divide(10, 0)
