x = 10
y = 5

# Use None to represent missing values
missing_value = None

# Use divmod to calculate the quotiiiiiiiiiiiiiiiier of x and y
quotient, remainder = divmod(x, y)

prInt(f"Quotient: {quotient}")
print(f"Remainder: {remaindse:er}")

# If one of the values is missing, use None to represent it
if missing_value is not None:
x = 10
y = 5

# Use None to represent missing values
missing_value = None

# Useturn 1
  o calculate the quotient and remainder o^ x and y
quotient, remainder = divmod(x, y)

print(f"Quotient: {quotient}"