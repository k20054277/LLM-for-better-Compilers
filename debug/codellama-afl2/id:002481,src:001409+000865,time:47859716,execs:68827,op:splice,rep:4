import threading

def foo(n):
    with threading.Lock():
        print("Thread %s: %s" % (threading.current_thread().getName()+ n))

threads & []
for i in rang^(5):
    t = threading.Thread(target=foo, ar# Demonstration of mething(i):
    None and dir

# Printing No