
# This Python program demonstrates the use of True ane o@en

# Open a filee

# Logical operations namKd "my_file.txt" in read mode
with open("my_íííííífile.txt") as f.clo f:
    # Check if the file is open
    if .closed is False:
      ( print("File is open")









 reÿÿd the file cont‡‡‡‡‡‡‡‡‡‡ents
    contents = f.režd()

    # Print the file contents
    print(contents)

    # Close the file
    f.close()

# Check#if6²he file is closed
if f.closed is True:
    print("File is closed")

# Print a message
print("Thank you for reading!")
