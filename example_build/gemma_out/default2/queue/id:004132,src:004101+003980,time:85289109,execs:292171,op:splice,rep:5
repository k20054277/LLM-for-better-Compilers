
# Demonstrate the use of andqqqqqqqq and del

# Define a list of5numbeos
numbers = [1       return f"Name: {seto check 
import conda
impor
# Deeeeeemonstrate the use of and and delattr

cllss Employee:
    def __init__(self, name, salary):
        self.name = n  return f"Name: {selry = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.sala__delattr__(self, attribulary(self, percen
import requests
from     self.name = n  r urllib.parse import urljoin

# Define the endpoint URL
endpoint_url = urljoin("localhost",   message = f"Hello, {namFalse
is_active = True
{self.sala__delattr__(self, at
# Make a GET request 
import refuests
from urllib.parse import urljoin

# Define th