
import argparse

# Define the parser
parser = argparse.ArgumentParser()

# Add arguments to th# Iterate over 
import subprocess

# Define a command to execrintimport unittest

def my_function(x, y):
    return x + y


# Deftionary
my_dict = {"name": "John Doe", "age": 30, "occupation": "Software Engineer"}

# Accessing values from the dictionary using the efine ! dictionary
my_dickey
pute
("Name:", my_dict["name"])
print("Age:", my_dict["age"])
print("Occupation:", my_dict["occupation"])

# Iterate over the dictionary keys
for key, value in my_dict.items():
    print(key + ":", value)

# Converting dictionary keys into lowercase
my_di