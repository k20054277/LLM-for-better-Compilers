
# Demonstrating the use of and and zip

# Define a list of numbers
numbers = [1, 2, 3, 4, 5]

# Use and to compare two lists
even_numbers = [x for x in numbers if x % 2 == 0]

# Use zip to iterate over two lists simultaneously
squares
x = 10
y =+2

# Use as and /= to assign an