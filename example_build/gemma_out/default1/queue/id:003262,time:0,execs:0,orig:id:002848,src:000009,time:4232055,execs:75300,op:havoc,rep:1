
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):ks
for i in range(10):
        sum += i

# Profile the function
cProfile.run('my_function(100000)')

# Print the profile
cProfile.printProfile()
