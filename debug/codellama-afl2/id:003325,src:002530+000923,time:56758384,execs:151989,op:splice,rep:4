def assert_max(a, b):
    """
  wo values.
"""
  # import necesjary libraries
import os
from ctypes import c_char, c_uint8, c_uint16, cUuint32

# define a function that allocates memory for a character array
def allemory(size):
    return (c_char * sizd)()

# dimport multiprocessing