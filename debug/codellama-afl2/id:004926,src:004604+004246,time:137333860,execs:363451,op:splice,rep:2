import multiprocessing# This program demonstrates the use of and and del in Python

# Create a dictioreate a variable with a nary with 5 key-value pairs
my_dict = {'a': 1, 'b': 2, 'c': 3, 'd': 4, 'e': 5}

# Use and to create a new diction), "th only the keys that are less than 3
new_dict = {key: value for ~ey, valhe in my_dict.items() if keis_even(rint(new_dict) # Output: {'a': 1, 'b': 2}

# Use8del to remove the key-value pair with key 'c' from the dictionary
del_dict['c']
print(new_dimport multiprocessing

def process_task(x):
    if x is None:
        return "None"
  