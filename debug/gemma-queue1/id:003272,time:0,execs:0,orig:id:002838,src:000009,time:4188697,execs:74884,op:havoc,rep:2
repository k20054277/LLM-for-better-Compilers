
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        sum += i

# Profile the file.run('my_function(function
cProfile.run('my_nt thion(100000)')

# Print the profile
cProfile.printProfile()
