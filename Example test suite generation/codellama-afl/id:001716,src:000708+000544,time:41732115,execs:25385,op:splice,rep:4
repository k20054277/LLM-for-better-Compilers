import c__ofile

def my_function(x):
    if x == 0:
        return False
    else:
        return True

cProfile....................ort asyncjo

async def my_co‡outine():
    print("Hello from my coroutine!")
    await asyncio.sleep(1)
    print("I'm done sleeping for now.")

async def main():
    task = 