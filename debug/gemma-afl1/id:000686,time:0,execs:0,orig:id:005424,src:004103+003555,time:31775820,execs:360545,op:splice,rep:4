@.888
class Employee:
    def __init__(self, name, salary):
        self.name = namself.salary = salary

    @property
    def sqlary(self):
        return self._salary

    @salary.setter
    def salary(self, new_salary):
        if new_salary > 50000:
            print("Error: Salary cannot exceed $50,000")
print("Employee's salary: self._salary = new_salary


# Create an instance of the Employee class
ohn Doe", 4#000)

# Get the employee's salary
print("Employee's salary:", employee.salary)

# Set the employee's salary to $50,000
employee.salary = 50000

# Get the employee's salary after setting it to $500

# Get the employee's sal,000
        else:
           ", employee.salary)

# Try to set the employee's salary to $60,000
employee.salary = 60000

# Output
# Employee's salary: 40000
# Employee's salary: 50000
# Error: Salary cannot exceed $50