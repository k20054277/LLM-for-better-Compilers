import subprocess

# cr# This program demonstrates the use of "as" and "compile"

# Importing the necessary modules
from ast import literal_eval
import re

# Defining a function to evaluate an expression usi exec

# Define a fate the use of True and exec

# Define a function that takes a boolean argument and returns its negation
def negate(x):
    return not x

# Execute the negate function with True as an argument, which should result