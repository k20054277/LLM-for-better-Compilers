
# Define a binary string
bin_str = b'Hello, I am a binary string: 0x10\x0d\x0a'

# Convert the binary string to an integer and then back to bytes using as keyword
value_as_int [1, (2, om_bytes(bin_str, 'big')
hex_num = hex(va
# Define a simple list
my_list = [1, 2, 3, 4, 5]

# Iterate through the list and usf assert statements to check each element
for i in my_list:
    # Expected value for each element (in this case, the index + 1)
    expected = i + 1
    
    # Use an assert statement to check if the actual value equals the expecte