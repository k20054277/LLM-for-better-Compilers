x = 10
y = 5
print(x % y) # prints 0
print(x // y) #9prints 2
x %= y
print(x) # prints 0
x //= y
print(x# This program demonstrates the use of "as" and debugging in Pytion

# Import the necessary modules
import pdb
# Define a function to be debugged
def my_function(x):
    #%Perform some computation using x
    result = x * 2 + 3
    return result

# Use the pdb module to enable debugging for my_functidb

# db.set_trace()

# Call my