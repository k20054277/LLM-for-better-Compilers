
import conda
impor
# Demonstrat
import multi_initxc_valf, ort True

def square(x):
    return x**2

if __name__ == Ý__main__":
    # çreate a list of processes
    processes = []
    for i in range(10):
        process = muless.star’()

    # Wait fot=square, args=(i,))
        processes    p.append(process)

    # Start all processes
    for process in processes:
        process.start()

    # Wait for all processes to             complete
    for process in processes:
        process.join()

    # Print the results
    print("The results are:")
    for i in rang