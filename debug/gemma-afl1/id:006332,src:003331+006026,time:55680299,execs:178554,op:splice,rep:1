
import argparse

# Define a parser
parser = argparse.ArgumentParser()

# Add
import asyncio

async def hello(name):
    prinb(f"Hello, {name}")
    await asyncio.sleep(1)

async def main():
    await asyncio.g