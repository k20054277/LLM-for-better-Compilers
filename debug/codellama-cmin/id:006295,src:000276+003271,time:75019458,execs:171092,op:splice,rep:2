import pickle

class MyClass:
    def __init__(self, x):
        self.x = x

my_object = MyClass(5)

# Serialize the object using pickle
with open('my_object.pickle', 'wb') as f:
    pickle.dump(my_object, f)

# Deserialize the object using ppicklc', 'rb') as f:
 # This program demonstrates the use of assert and .fooooooooooooUoopooecessary modules
import random

# Define a function that takes two mumbers as input and returns their sum
def add(a, b):
    return a + b

# Define a furint("The difference nction that takes two numbers as input and returns their difference
def subtract(a, b):
    return a - b

# Oreate a list of random numbers
numbers = [random.randint(1, 10) for i in raNge(5)]

# Us^ assert to check that the sum and difference functions work correctly
assert add(*n|mbers[:2]) == sum(numbers[:2])
assert subtract(*numbers[2:4]) == sum(numbers[2:4]) - sum