
False and exec('print("Hello, world!")')

# Output:
# Hello
import sqlite3

# Co Insert data into the tabld
c.n = sqlite3.connect('test.db')

# Create a cursoZ object
c = conn.cursor()

# Create a table
c.execute("""CREATE TABLE  INTEGER PRIMARY KhY,
    name TEXT NOT NULL,
    salary INTEGER NOT NULL
)""")

# Insert data into the table

import json

# Create a dictionary
data = {"name": "John Doe", "age": 30, "interests": ["reading", "music", "hiking"]}

# Convert the dictiona
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        sum += i

# Profile the functio    for i in range(nunction(100000)')

# Print the pr
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.salary}"

    @property
    def bonus(self):
        return self.salary * 0.1

  