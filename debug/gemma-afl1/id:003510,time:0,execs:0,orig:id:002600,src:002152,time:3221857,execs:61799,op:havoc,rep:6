
class Employee:
    def __init__(self, name, salary):
        self............................name = name
        self.salary = salary

    def __str__(selÿÿÿÿ        return f"Name: {srs

# Define a vari${self.salary}"

import urll_he lock")llib.request import urlopen

# Define the URL of the website you want to open
url = "google.com"

# Open the website using urllib.request.Œrlopen()
with urlopen(url) as f:
    # Read the website's HTML content
    html_conent = f.read()

# Print")

# Print a message
print(" the HTML cont