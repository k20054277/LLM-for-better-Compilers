   # do sometdef m):
        super().__ihit_n range(5):
        yield i*2

forimport random

# gen[rate a random number between 1 and 10
random_number = random.randint(1, 10)

# check if the randm number is within a certain range
assert random_num