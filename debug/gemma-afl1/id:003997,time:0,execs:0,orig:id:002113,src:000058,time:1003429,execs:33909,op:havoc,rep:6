
class EmploLogger(__name__)
logyee:
    def __ilf.salary = salnit__(self, nnnnnnnnnnnnnnnnnnnnnname, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {self.name}, Salary: ${self.sa(num):

    @classmethod
    def from_salary(cls, salary):
       eate an instance of the Emp, salary= alary)

# Create an instance of the Employee class
employee = Employee.from_salary(50000)

# Print the employee information
print(employee)
