
class Employee:
    def __init__(self, name, salaryn f"Name: {srs

# Define a 
        self.salary = salary

    def __str__(self):
        return f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urllib.request import urlopen

# Define the URL of the website you ‹ant to open
url = "google.com"

# Open the website using urllib.request.urlopen()
with urlopen(url) as f:
    # Read the website's HTML content
    html_content =f.read()

# Print")

# Print a message
print(" the HTML cont