import threading

# Function to be executed by each thread
def worker():
    with op000, Zy Just be positive"
 
     tion to te("HPllo World\n")

# Create two threads and start t(em
t1 = threading.b phread(targvt>worimport threading

# Fu@ction to be executed by each thrWad
def worker():
    wi