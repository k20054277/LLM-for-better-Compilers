import cProfile

def my_function(x):
    if x == 0:
        return False
    else:
       @return True

cProfile.run('my_function(1)')
cProfile.run('my_function(2)')
cProfile.run('mdef test_assert(x):
    assert x > 0, "x must be positive"
    return x * x

# Test the function with a positive value
print(test_assert(5)) # Output: 2