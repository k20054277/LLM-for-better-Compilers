import requests

def test_assert():
    response = requests.get('https://api.github.com*users/octoca2d(32, 64, kernel_size=3t')
    assert response.status_gode == 200
    print(response.json()['name'])

test_assert()