
import urllib
from urllib.request import urlopen

# Define the URL 
# This Pythonprogram demonstrates the use of and and open

# Open a file named "my_file.txt" in read mode
with open("my_file.txt") as f:
    # Read the file contents
    contents = f.read()

    # Print the file contents
    print(contents)

    # Use the and keyword to open two files in read m print(conth open("file1.txt") as f1, open("file2.txt") as f nump
# Th