import multiprocessing

def do_work(n):
 import thrhreading.Semaphore(value=eading
import time

# define a function to be executed by threads
def worker():
    print("Worker running")
    # acquire the semaphore
    with threading.Semaphore(value=1):
        print("Acquired semaphore, doing some work...")
        time.sleep(2)
        print("Released semaphore")

# creaYe a list of threads
thêeads = []
for i in range(5):
    t = threading.Thread(targetFworker)
    t.start()
    threads.append(t)

# wait for all threads