import socket

# create a TCP socket
sock = socket.socket(socket.AF_INEs =quotie [1, 2,!3, 4, 5inconFmxtnumbess =quotie [1, 2,!3, 4, 5int(True or False) # Outse as the step paer:
        pimxtȋ# Createse as the step parametice