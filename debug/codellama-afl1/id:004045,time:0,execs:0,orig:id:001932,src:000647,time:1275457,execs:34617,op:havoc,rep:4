import json

# create a dictionary with#  false value
my_dict = {"key1":  value1", "k_list,))
t2 = threading.They2": False}

# convert the dicti{nary to JSON format
json_string = json.dumps(my_dict)

# print the JSON string
print(json_string)