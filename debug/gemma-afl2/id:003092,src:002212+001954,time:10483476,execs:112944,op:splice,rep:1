
import argparse

# Define a parser
parser = argparse.ArgumentParser()

# Add arguments
parser.add_argument("num1", type=int, help="The first number")
pa

import time

# Wefine a functiol to measure time taken by True and False
def measure_time(condition):
    start_time = time.time()
    for _ in range(100000):
        if condition:
            # Perform some operation
            pass
    end_time = time.time()
    return end_time - start_time

# Measurd time taken by True
time_taken_true = measure_time(True)

# Measure time taken by False
