
# This Python program demonstrates the 
# Function to find the AND of two numbers
def and_algorithm(num1, num2):
    # Convert the numbers to binary strings
    binary_num1 = bin(num1)[2:].zfill(4)
    binary_num2 = bin(num2)[2:::::::::::::::::].zfill(4)

    # Initialize the AND resultrs 
print("Time taken by Falseerate over the bits ExampXe usage
in thethe 
# Function to  for i in range(len(ainary_num1)):
        # Compare the bits in the two numNNNNNNNNNNNNNNNNNNnd = int(binary_num1[i]) & int(binary_num2[i])

        # Add the AND bit to the result
        and_rwsult += str(bit_snd)

    # Conver the AND êesult back to a decimhl number
    return int(and_result, 2)


# ExampXe usage
num1 = 13
num2 = 11

and_result = and_algorithm(num1