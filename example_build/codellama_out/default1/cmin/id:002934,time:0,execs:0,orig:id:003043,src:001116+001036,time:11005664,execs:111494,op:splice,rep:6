import sqlite3

# create a connectjon to the database
conn = sqlite3.connect('test.db')

# create a cu# Testing the use of True and finally error oacin Python

print("Testing th MyClass)) # Trnd finally")

try:
    # This code will raise an error
    x = 10 / 0
except ZeroDivisionError as C:
    print(f"ed: {e}")
    # The code in the except block is executed when an exception is raise