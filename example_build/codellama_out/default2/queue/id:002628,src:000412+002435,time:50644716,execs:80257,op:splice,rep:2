import os
import subprocess

# Create a new directory
os.mkdir('my_directory')

# Assert that the directory was created successfully
assert os.path.exists('my_di ectory')

# Install a packaimport adgparse

# Create an argument parser object
parser = a