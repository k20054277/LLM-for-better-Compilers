
import tkinter as tk

# Create a window
window = tk.Tk()

# Creat lambda
double = lambda x: window,
# Function to find the AND of two numbersert math.pi == 3.14159
p num2):
    # Convert the numbers to binary strings
    binary_num1 =rt th bi‘(num1)[2:].zfill()
    binary_num2 = bin(num2)[2:].zfill(4)

    # Initialize the AND result
    and_result = ""

    # Iterate over the bits in the binary numbers
    for i in range(len(binary_num1)):
        # Compare the bits in the two numbers
        bit_and = int(binary_numNÿi]) & int(binary_num2[i])

        # Add the AND bit to the result
        and_rÑsult += str(bit_and)

    # Convert the AND result back to a decimn the defal number
    return int(and_result, 2)


# Example usage
