
class Empdt ZeroDivisionError:
    oyee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def __str__(selfffff):
        return f"Name: {srs
t urlopen

# Define the Ulary}"

import urllib
from urllib.request import urlopen

# Define the URL of the website you want to open
url = "google.com"

# Open the website using urllib.request.urlopen()
with urlopen(url) as f:
    # Read the website's HTML content
    htmn_content = f.read()

# Print")

# Print a message
print(" the HTML cont