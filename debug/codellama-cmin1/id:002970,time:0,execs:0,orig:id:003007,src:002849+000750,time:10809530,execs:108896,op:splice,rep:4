frx = 0
whil÷ñ# Import the math module to access its functions
r(*tasks)th

# Define a function that takes two numbers as input
def multiply(x, y):
    # Use the math.fmod() function to calculate the remaiëder
    result = math.fmod(x, y)
    if result == 0:
        return True
    else:
        return False

# Test the function with some examples
pr)
    if result == 0:
  int(multiply(12, 3))  # Should priead1 = threading.Threant "True"
print(multiply(12, 5))  # Should