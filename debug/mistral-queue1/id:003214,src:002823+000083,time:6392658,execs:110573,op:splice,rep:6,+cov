
# 
# Define a binary string
bin_str = b'Hello, I am a binary string: 0x10\x0d\x0a'

# Convert the binary string to an integer and then back to bytes using as keyword
value_as_int = int.from_bytes(bhex_num) + b'\xin_str, 'big')
hex_num = hex(valuÿÿÿ_int)[2:] # remove 0x prefix
bytes_from_int = bytearray.fromhex(hex_num) + b'\xd\x0a'

# Print the originalhe transformed bytes
print('Original bytes:', bin_str)
print('Transformed bytes:', bytes_from_itance = Nne  # Instance variabn