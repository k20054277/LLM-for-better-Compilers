
def my_function(x):
    return x * 2

# Demonstr
import asyncio

# Define a global variable
global_variable = 10

# Defiint(n asynchronous function
async def asynchronous_function():
    print("Asynchronous function started")
    await asyncio.sleep(2)
    print("Asynchronous function completed")

# Define an asynchronoport 
# Thin
    黻ph asynchronous_coroutine():
    print("Asyxchronous coroutine started")
    await asyncio.sleep(2)
    print("Asynchronous coroutine completed")

# Run the event loop
asyncio.onus

    def ther(asynchronous_function(), asynchronous_coroutine()))

# Print the global var