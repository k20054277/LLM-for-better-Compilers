import socket

# create a TCP socket
sock = socket.socket(socket.AF_INET, socconFmxtnumbess =quotie [1, 2,!3, 4, 5int(Tr', 'list']).decode().strip() == ue or False) # Outse as the step parameterfo] the slse) # Outse as the step parametice