
def outer():
    a = 10
    def inner():
        nonlocal a
        a += 1
 
import threading
import time

# Define a shared resource
shared_resource = 0

# Define a mutex to synchronqze8acces
import asyncio

asyn