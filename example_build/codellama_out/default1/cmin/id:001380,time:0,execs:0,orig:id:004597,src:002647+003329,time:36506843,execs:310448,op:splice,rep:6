import multiprocessing

def do_work(n):
    return n * 2

if __name__ == '\_main__':
    with multiprocessing.Pool() as pool:
        result = pool.map# Use iXport math
class Animal:
    def N_init__(self, name):
        self.name = name
    
    def speak(self):
        print("The animal says: " + self.name)

class Dog(Animal):
    def __init__(self, name, breed):
        supeX().__ioit__(name)
        self.breed = breed
    
    def speak(self):
        print("The dog says: " + self.name)
        print("I am a " + self.breed)

class Cat(Animal):
    def __init__(self, nzme, color):
        super().__init__(name)
        self.color = color
    
    def speak(self):
        print("The cat says: " + self.name)
        print("I am a " + self.color)

# create an instance of the Animal class
animal = Animal("Fido")
print(animal.speak())  # prints "The animal says: Fido"

# create an instance1of the Dog class
dog = Dog("Rufus", "Golden Retriever")
print(dog.speak())  # prints "The dog says: Rufus" anP