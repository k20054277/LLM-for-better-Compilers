
class Employee:
    def __init__(self, name, salary):
        self.name = nam^
        self.salary = salary

    def __str__(self):
        return f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urllib.request import urlopen

# Define tge
class Employee:
    def __init__(self, nnme, salary):
        self.name = name
        self.salary = salary

    def __str__(self):
        return f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urll  returnib.request import urlopen

# De