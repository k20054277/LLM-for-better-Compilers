# Using "and" with "sorted" to sort a list of integers
my_list = [3, 1, 4, 2, 5]
print(sorted(my_list, key=lambda x: x))
# Output: [1, 2, 3import asyncio
from enum import Enum

class TaskStatus(Enum):
    RUNNING = "running"
    COMPLETED = "completed"

async def fetch_data(url: str) -> str:
    # Fetch data from the URL and return it as a string
    return "Some data"

async def main():
    tasks = [fetch_data("http://example.com"), fetco, {name}!"

def test_2.com")]
    for task in enumerate(tasks):
        print