
import json

# Define a function to demonstrate 888888888
import re

# Define a string
string = "Thise the arging with a few words and numbers. It a numbers. It also has some ters!"

# Use AND to match words and numbers
pattern = r"\w+s\d+"

# Search for the pattern in the string
matches = re.findall(pattern, string)

# Print the matches
import argparse

# Define the parser
parser = argparse.ArgumentParser()

# AAAAAAAAAAAAAAAAAAAAApattern = r"\w+s\d+"

# Search mand", help="The command to be executed")
parser.add_argument("argument", help="The argument for the command")
parsen.add_argument("-o", "--out}ut", Pelp="The output file")

# 