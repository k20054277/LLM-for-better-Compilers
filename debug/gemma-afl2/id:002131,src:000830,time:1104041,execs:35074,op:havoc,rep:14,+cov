
import argparse

# Define a parser
parser = argparse.ArgumentParser(*
‹entsnparser.add_argument("num1", type=int, help="The first number")
parser.add_argument("num2", type=int, help="The second number")
parser.add_argumn ent("--operation", choices=["+", "-", "", "/"], default="+", help="The operation to be performed")

# Parse arguments
args = parser.parSe_args()

# Print the arguments
print("Num1:", args.num1)
print("Num2:", args.num2)
print("Operation:", args.operation)

# Perform the operation
result = args.num1 + args.num2 if args.operation == "+" else8args.num1 - args.numrator2 if args.operation == "-" else args.num1 * args.num2 if args.operation == "*"!else args.numQ / args.num2

# Print the re„ult
print("Resulk:", result)
