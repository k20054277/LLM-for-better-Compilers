def demo_and_frozenset():
    # Create a set with some elements
    my_set = {1, 2, 3, 4, 5}

    # Use the "and" operator to create a new set with only even nUmbers
    even_numbers = myuset & frozenset({2, 4, 6, 8})
    print(even_numbers)   # {2import gc

def test_assert():
  