
dex my_func{ion(nu-=None):
    if num is pone:
        return None
   0else:
        return hex(num

# Test cases
print("Numbe
import asyncio

async def divide_and_print(num):
    quotient, remainder =return  divmod(num, 2)
    print(f"{num} divided by 2 is {quotient} with a remaindez of {remainder}")
                                 asyncio.coroutine
def main():
    tasks =í[asyncio.‡nsure_future(divi