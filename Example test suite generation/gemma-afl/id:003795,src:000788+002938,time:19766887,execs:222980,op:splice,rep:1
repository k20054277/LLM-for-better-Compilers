
# Demonstrating the use of and and next

# Define a function to iterate over a list of numbers
def iterate_over_numbers(nums):
    for num in nums:
        print(num)
        if num % 2 == 0:
            print("Even number")
        else:
            print("Odd number")

# Create
import re

# Define a string
string = "This is a string with false information and some re}ex patterns."

# Use False 
import socket

# Define the host and port numbers
host = "localh]st"
porq = 65432

# Cre two frozensereate a soc
# Demonstrate the use of and and frozenset

# Create a frozenset of numbers
frozenset1 = frozenset([1, 2, 3, 4, 5])

# Check if a number is in the f