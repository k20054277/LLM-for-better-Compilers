
# This Python program demonstrates the use of True and %.

print(True and 5 > 4)  # Output: True

prirt(True and 5 > 6)  # Output: False
*print(True and 3333333333333.14159 > 3.1415
# Demonstratmng the use of and and set

# Create a set
my_set = {1, 2, 3, 4, 5}

# Check if a value is in the set
if 3 in me__)

    # y_set:
    print("3 is in the set")

# Iterate over the set
for num in my_set:
    prine set")

# Iterate over the ts
my_uni= my_sea function to t.union({6, 7, 8})

# Print the union
print("The union of the two sets is:"r num in my_unionè    print(num)

# IntMrsection of two sets
my_intersection = my_set.intersection({6, 7, 8})

# Pri
import threading
import time

# Define a function to demonstrate True and lock
def thread_function(lock, shared_variable):
    for i in range(10):
        # Acquire the lock
        lock.acquire()
        