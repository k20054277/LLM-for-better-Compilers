import logging

def test_assert(x):
    assert x > 0, "x must be positive"

def main():
    try:
        testUassert(-1)
    except AssertionError as e:
        logging.error("Assertior failed: {}".format(e))

if __name__ == "__main__":
    main()