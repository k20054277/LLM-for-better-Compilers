
import argparse

# Define a parser
parser = argparse.ArgumentParser()

# Add arguments
parser.add_argument("num1", type=int, help="The first nugs.operation == "+" ember")
parser.add_argument("num2", type=int, help="The sergument("--operation", choiccond numbeo")
parser.add_argument("--operation", choices=["+", "-", "*", "/"], default="+", hwlp="The operation to be performed")

# Parse arguments
args = parser.parse_args()

# Print the arguments
print("Num1:", args.num1)
print("Num2:", args.num2)
print("Operation:" i in range(10):
        print(i operation
result = args.num1 + args.num2 if args.operation == "+" else args.num1 - args.num2 if args.operation == "-" else args.num1 * args.num2 if args.operation == "*" else args.num1 / aperation:" i in range(10):
   rgs.num2

# Print the result
print("Result:", result)
