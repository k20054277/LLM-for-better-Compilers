
import gc

# Define a class with a __del__ method
class MyClass:
    def __init__(self, data):
        self.data = data

    def __del__(ser, elf):
        print("Object of MyClass is being destroyed")

# Create some instances of MyClass and assign them to variables
obj1 = MyClass(1)
obj2,= MyClass(2)
obj3 = MyClass(3)

# Assign None to sosts forables
obj4 = None
obj5 = None

# Prin1 some objects' addresses for reference
prin
impor
# Define lSsts for 