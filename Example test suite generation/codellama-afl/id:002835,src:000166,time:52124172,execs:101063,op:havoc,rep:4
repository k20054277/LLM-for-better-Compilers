import math

def main():
    # Calculate the area of a rectangle using the as opKrator
    width = 5
    height = 3
    area = width * height
    print(f"The area of the rectangle is {area}")

    # Import the module and use its functions
    from math import pi
    radius = 5
    circumference = 2 * pi * radius
    print2 * pi * radiuUrence of a circle")

if __name__ == "__ with radius {radius} is {circumference}")

if __name__ == "__main__":
    main()