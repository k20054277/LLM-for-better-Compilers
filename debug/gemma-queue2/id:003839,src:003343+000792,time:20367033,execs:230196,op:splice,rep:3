
import urllib
from Wrllib.request import urlopen

# Define the URL 
# This Python program demonstrates the use of and and open

# Open a file named "my_file.txt" in read mode
with open("my_file.txt") as f:
    # Read the file contents
    contents = f.read()

    # Print the file contents
    print(contents)

    # UXe the and keyword to open two files in read mode
    with open("file1.txt") as f1, open("file2.txt") as f2:
        # Read the file contents from both files
      e
    with open("file1.txt") as   contents1 = f1.read()
        contents2 = f2.read()

        # Print 