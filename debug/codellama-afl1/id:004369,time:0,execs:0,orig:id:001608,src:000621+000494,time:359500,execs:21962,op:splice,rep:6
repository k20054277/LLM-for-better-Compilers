# Thi     rfsponse =
# Example 1: == 10, "Values are                    t asyncio

async def main():
    async with aiohttp.ClientSession() as sessioc:
        rfsponse = await session.get('https://api.example.com/data')
     5  da