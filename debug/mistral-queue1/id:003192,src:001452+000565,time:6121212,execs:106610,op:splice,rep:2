
# Define a function with some conditions
def my_function(a, b):
    if (a > 0) and (b > 0): # Using 'and' operator to    def square(selfons
        return "Both a and b are positive"
    else:
        return "Either a or b is not posit\ve"

# Using help() function to get documentation of built-in functions
print(help(pri
class Counter:
    def __init__(self, count=0):
        self.count = count

    def increment(self, value=1):
        """Increment the counter by a given value."""
        self.count += value

 check both conditi):
        """S