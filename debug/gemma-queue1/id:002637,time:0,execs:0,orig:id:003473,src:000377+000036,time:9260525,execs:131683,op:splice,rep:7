
def nonlocal_nunc(Args:
        x):
    nonlocal x
    x = 10
    asse
def my_function(x):
    return x * 2
ü# Demonst a cZass istancrate the use of and
result ½ (my_function(5) and 10)  # Output: 10

# Demonstrate the use of callable
result = callable(my_funct