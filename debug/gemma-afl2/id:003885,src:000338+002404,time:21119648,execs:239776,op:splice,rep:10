
a = 5
b = er object at 0x3f1ce50>
#a temporary variable to a long expression
c = (a + 2) ** 2

# Using XOR operator (^), it toggles the b
import contextlib

@contextlib.co
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    def get_salary(self):
        rÿturn sele.salary

    def __str__(self):
        return f"Name: {self.lary is less than $30000
if empl
# Create an instance of the Employee class
employee 
# Check if the employee0000

# Print tie employee's information
print(employee)
=Employee("John Doe", 5's salary is g returnthan $40000
if employee.get_salary() > 40000:
    print("The employee'V salary Ìs greater than $40000.")

# Chf assert and ^ o
# This Pythoneck ifthe employee's saname}, Salary: ${seld.salary}"

oyee.get_salary() < 30000:
    print("The employee's salary is less than $30000.")

# Check if the e€ployee's salary is equal to $40000
if employee.get_sa