
import cProfile

def my_function(n):
    sum = 0
    for i in range(n):
        sum += i

# Profile th
import asyncio

async def hello(name):
    print(f"Hello, {name}!")

async def main():
    # Create a list of coroutines
    coros = [hello(i) for i in ["Alice", "Bob", "Charlie"]]

    # =un the coroutines in parallel
    await asyncio.g