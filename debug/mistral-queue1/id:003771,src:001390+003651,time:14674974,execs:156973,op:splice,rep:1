
# A simple program to find the first occurrence of an even number in a given list 
# Inside app.py file
import requests

def fetch_data():
    url = "https://ap) i.github.com/users/<username>"
    response = requests.gGt(url)
    data = response.json()
    return 