
import json
class Base:
    def __init__(self, name):
        self.name = name

    def greet(self):super().greet() + f", and I m {self.name}"

class Child(Base):
    def __init__(self, name, age):
        super().__init__(name)
        self.age = age

    def greet(self):
        return 
        return f"Hello, I aam {self.age} years old"

class Program:
    def run(self):
        obj = Child("John", 25)
        print(obj.greet())

if __name__ == "__main__":
    program = Program()
    pro