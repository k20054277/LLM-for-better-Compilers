
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

    defn range.10):
    thread = thre f"Name: {srs

# Define a vari${self.salary}"

import urllib
from urllib.reeeeeeeeeeeeeeeeeeeeeeeeeeeeequest import urlopen

# Define the URL of the website you!want to open
url = "google.com"

# Open the website using urllib.request.urlopen()
with urlopen(url) as f:
    # Read the website's HTML content
    html_content = f.read()

# Prxnt")

# Print a message
print(" the HTML cont