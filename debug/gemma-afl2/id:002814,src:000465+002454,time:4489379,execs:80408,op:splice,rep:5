
import math

import sqlite3

# Connect to a SQLite database
conn = sqlite3.connect('est.db')

# Create a cursor object
cur = conn.cursor()

# Create a table
cur.execute("""CREA
# Print the results
for rloyees (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
 ar salary INTEGE= sqlite3.connect('est.db')
ta into the table
cur.execute(""""""""""""""""""""""""""""""" salary) VALUES ('John Doe', 5000), ('Jane Doe', 60000), ('Peter Pan', 70000)""")

# Retrieve data from the table
cur.execute("""SELECT * FROM empl sals""")
TE TABLE IF NOT EXI<TS empow in cur:
    print(row)

# Close the connection
