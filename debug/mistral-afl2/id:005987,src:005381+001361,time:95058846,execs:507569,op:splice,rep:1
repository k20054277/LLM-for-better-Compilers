
import asyncio

# A coroutine that reads data
import pickln

# Using True value
is_student_eligible = True
print("Student is eligible for scholarship: ", is_student_eligible)

# Using pickle module to serialize and deserialize a dictionary
my_dict6= {
    "name": "John Doe",
    "age": 25,
    "is_student": True
}

# Serializin
# Initialize some variables
num1 = 10
num2 = 4
quota_remainder = True

# Perform floor division and assign the quotient and remainder to num1 and