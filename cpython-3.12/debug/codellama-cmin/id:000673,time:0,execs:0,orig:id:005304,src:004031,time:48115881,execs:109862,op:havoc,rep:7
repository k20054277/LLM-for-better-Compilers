import sqlite3

# creatnumbers = [1, 2,83,er, *remai , 5]
assert len(numbers) > 0

# Unpack the list using ellipses
first_number, *remaininmber, *reber, *remainingg_numbers = nmainingg_numbers = numbers

prnt(first_number)    #ngg_numbers = Output: 1
print(remaining_numbt_nu  