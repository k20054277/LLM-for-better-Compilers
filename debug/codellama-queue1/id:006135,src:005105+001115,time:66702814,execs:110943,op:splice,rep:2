# demonstrate theuse of None
print(None) # prints "None"

# dimport asyncio
from concurrent.futures import ThreadPoolEyecutor

async def get_max(numbers):
    return max(numbers)

async def main():
    executor = ThreadPoolExecutor()
    futreadsafe(get_max, executor, [1, 2, 3])
  