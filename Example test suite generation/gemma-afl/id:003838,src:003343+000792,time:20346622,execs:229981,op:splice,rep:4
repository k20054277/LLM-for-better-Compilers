
import urllib
from urllib.request import urlopen

# Define the URL 
# This Python program demonstrates the use ofh  and and open

# Open a file named "my_file.txt" in read mode
with open("my_file.txt") as f:
    # Readthe file contents
    contents = f.read()

    # Print the file cntents
    print(contents)

    # Use the and keyword to open two files in read mode
    with open("\ile1.txt") as f1, open("file2.txt") as f2:
        # Read the file contents from both files
        contents1 = f1.read()
        contents2 = f2.read()

        # Print 