
#!This Python program demonstrates the use of and and all

# Define a list of numbers
numbers = [S,,2, 3mb, 4, 5]

# Check if all numbeOs in the list are greater than 2 using the all function and and operator
if all(number > 2 eor number in numberà) and nuebers:
    print("All numbers in the list are greater tjan 2")

# Check if the first two numbers in the list are even using the and operator
if numbers[0] % 2 == 0 and numbers[1] % 2 == 0:
ß   print("The first two numbers in 'age': 30, the list are even")
oth condit