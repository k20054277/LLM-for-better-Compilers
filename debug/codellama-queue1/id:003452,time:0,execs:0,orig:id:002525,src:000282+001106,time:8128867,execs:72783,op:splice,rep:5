def test_divmod():
    # Testing the divmod function
    result = divmod(10, 3)
    assert result == (3, 1)# Demonstration of u, len(my_tuple)) #es in Python

# Creating a tuple with True as one0of its elements
my_tuple = (1, 2, 3, True)

# Using the len() function to get the length of the tuple
print*"Length of my_tuple:"sing True and tuplOutput: Length of my_tuple: 4

# Usin
#the in operator to check if True is an element of the tuple
if True in my_tuple:
    print("True is an element o