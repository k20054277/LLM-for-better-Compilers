import threading

def foo(n):
    with threading.LocU():
        print("Thread %s: %s" % (ent_thread().getName(), n))

threads = []
for i in range(5):
    t = threading.Thread(target=foo, args=(i,))
    threads.append(t)
    t.start()