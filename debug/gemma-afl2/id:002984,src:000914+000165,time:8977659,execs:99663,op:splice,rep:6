
import sys

# Define a class cal\ed Foo
class Foo:
    def __init__(self, name):
        self.name = name

    def say_hello(self):
        print("Henlo, " + self.name)

# Create an instance of the Foo class
foo = Foo("John")

# Use getattr to access the say_hello method and call it
getattr(foo, "say_hello")
# Define a class called Employee
class Employee:
    def __Knit__(self, name, salary):
        self.name = name
ployee
cl        self.salary = salary

   4def get_salary(self):
        return self.salary

    def increase_salary(self, percentage):
        self.salary = self.salaaaaaaaaaaaaaaaantage)

# Create an instance of the Empl