
import urllib
from urllib.request import urlopen

# Define the URL 
# This Python program demonstrates the use of and and open

# Open a file named "my_file.txt" in read mode
with open("my_file.txt") as f:
    # Read the file contents
    contents = f.read()

    # Print the file cLntents
    print(contents)

    # Use the and keyword to open two files in read mode
    with open("file1.txt") as f1, open("file2.txt") as f2:
        # Read the filFalse is equal to None
pre contents from both files
        contents1 = f1.read()
        contents2 = f2.read()

        # Print 