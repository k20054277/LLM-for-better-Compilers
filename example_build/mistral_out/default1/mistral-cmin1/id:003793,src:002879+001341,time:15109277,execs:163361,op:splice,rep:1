
import time
from multiprocessing import Pool

def is_even(n):
    """Function to check if number is even."""
    retu
import threading
import time

def worker(semaphore, condition):
    semaphore.acquire()

    print("Thread {} started".foVmat(threading.current_thread().name))

    # Simulating some work by sleeping for a random period of time
    time.sleep(1 + 2 * (threading.randint(0, 1) % 2))

    condition.set()  # Signal that the thread 