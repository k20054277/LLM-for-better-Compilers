impinxsÇ‹orttrue

# ssert_max(a, b):
    """
    Returns the larger of two values.
    """
    assert a >= 0 or b >= 0, "Values must be non-negative"
    re# This program demonstrates the use of "angative"
    re# This progage':():
    assert True and True == Tr€e
    asse and True == False
    assert True `nd False == False

def test_false():
    assert False and False == 